# Intended CI job set for .github/workflows/ci.yml.
# guard_required_checks_match.py asserts this name set == the jobs defined in ci.yml.
# When you add/rename/remove a job, update this file in the SAME commit, and
# update the GitHub branch-protection "required status checks" rule to match the
# non-advisory entries below.
#
# name            # advisory  -> runs in CI but is NOT a merge blocker
# name                        -> merge blocker (add to branch protection)

backend
frontend
mobile
mobile-apk        # SR-31 — builds the pilot APK artifact; a broken build blocks merge
mobile-emulator-smoke # advisory  — SR-35 headless emulator smoke; flip to blocking when stable
e2e               # merge blocker — 2026-09-11: promoted from advisory. Was "no API, thin
                  # coverage (74 tests)"; now covers every one of 130 screens (141 tests)
                  # across every domain, so a broken import/crash/role-gate regression is
                  # caught here, not just in e2e-golden's narrow 5-test golden path.
e2e-golden
docker
load-harness      # advisory  — presence check only, not a soak
postgres-rls      # merge blocker — D16 (2026-09-12): promoted from advisory. Test
                  # selection widened to tests/test_rls_coverage.py + tests/tenancy/
                  # (was 2 -k-filtered files; the RLS-policy-forced check never ran).
                  # Test-strictness only — Table B's POSTGRES_RLS_ENABLED=0 stands.
determinism-probe # advisory  — suite with frozen clock + network ban; flip to blocking when green
invariant-sweep   # Phase 2   — merge blocker: Phase 2 gate AND the full-suite strict sweep (both blocking as of 2026-09-10)
real-broker      # advisory  — QOS-0012 chains against a real Celery broker (Redis), not eager mode
mutation-audit    # advisory  — QOS-0019 mutmut on the crown-jewel modules; survivors are missing assertions, not a gate
qos-lint          # merge blocker — Q-OS backlog schema / evidence cap / lifecycle / doc-sync + linter selftest
qos-dashboard     # advisory  — appends the Q-OS dashboard trend snapshot as an artifact (main only)
flow-catalog      # advisory  — Graph 1 extractor + generated FLOW_CATALOG.md; 2 green weeks then web lead flips
writer-impact     # advisory  — Graph 2 P2-T6 writer-diff nudge; flips blocking with Phase 6 (S5b), separate clock from flow-catalog's A3
