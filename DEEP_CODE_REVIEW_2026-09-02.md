# BizBoard — Deep Line-by-Line Code Review (2026-09-02)

**Reviewer:** Claude (Sonnet 5), on request "deep code review each line by line, list all bugs/defects/gaps/UX issues, log as many points as possible."
**Scope:** `backend/` (Django/DRF, ~60k LOC), `web/` (React/TS, ~73k LOC), `mobile/` (Capacitor), CI/config.
**Method:** Reading production source directly (models, services, serializers, views, tasks, utils, pages, api clients). Migrations and lockfiles are not treated as product bugs unless they encode a broken policy. Findings that were already fixed in-tree are not repeated; this doc is additive to the prior `COMPREHENSIVE_CODE_REVIEW_FINDINGS.md` / `UX_AUDIT_REPORT.md`.

**Severity key:** `P0` data loss / money wrong / security hole · `P1` incorrect behaviour on a real path · `P2` gap / partial feature / fragile · `P3` polish / nit / suggestion.
**Status:** First pass complete for the money/tax/accounting/payments/GST spine + FE money math + API client. See Appendix for coverage; several large modules still want a dedicated pass.

### Remediation progress (2026-09-03)

Environment note: local `.venv` was dead (no real Python 3.12); rebuilt via `uv` (Python 3.12.11, deps within `constraints.txt`). **Pre-existing baseline: 46 backend tests fail on the current WIP working tree before any remediation** (`46 failed, 951 passed`, same under `--migrations` and `--nomigrations`) — the Wave 0/A-D WIP is itself mid-implementation. All remediation is verified as "no new failures vs that 46-test baseline".

**MILESTONE (2026-09-03): backend + frontend fully green.** Backend `pytest` (with migrations): **999 passed, 0 failed** — from a 46-failure pre-existing baseline. Frontend: `tsc` 0 errors (from 6), `vitest` 189/189. `ruff` clean. `makemigrations --check` clean. All 46 pre-existing WIP failures + ~70 review findings fixed, 0 regressions.

**Phase-5 update (2026-09-03):** RLS full rollout (106 tables + `rls_bypass` GUC for beat tasks + coverage test, `POSTGRES_RLS_ENABLED` default ON), real curated HSN/SAC rate catalog (234 rows / ~117 codes, pre- and post-GST-2.0 windows, CA-verify disclaimer), filing-grade payroll (EPS/EPF split + admin A/c 2 + EDLI A/c 21, per-state PT for 25 states incl. no-PT, annualised sec-192 new-regime TDS), plus the self-contained batch below.

**Phase-6 update (2026-09-03): the P2/P3 long tail + the domain features.** ~40 more findings closed across 5 commits (`97c20c8` → `8e658d2`). Backend **1007 passed / 0 failed** (`--nomigrations`); `ruff` + `makemigrations --check` clean; FE `tsc` 0 / `vitest` 189/189.
- **New subsystem — GST-08:** `BillOfEntry` model + `/api/v1/purchases/bills-of-entry/` CRUD + complete/cancel + GL posting; `build_gstr3b` now carries a real `import_itc` (4(A)(5)) block and folds import IGST/cess into available ITC.
- **ACC-09:** WDV / block depreciation (`method`, `salvage_value`, `wdv_annual_rate`, `block_key`; SLM base = cost−salvage; final-month true-up; CA-verify the block rate).
- **SUB-01:** data migration grandfathers every pre-billing tenant into a 60-day TRIAL.
- **PAY-03/04:** PayU S2S callback parsed as form-encoded; reverse hash fills udf1..10 + additionalCharges; v2 payment-links API (still needs a live PayU merchant to verify the hash layout).
- **INTG-02/03:** AA matcher now keys on a UTR/RRN from the narration + a unique amount+date candidate, each row in its own short transaction. **INTG-01:** `ReBITClient` scaffold with the real FI request/fetch sequence, fail-closed; decryption left `NotImplemented` (no unreviewed crypto shipped).
- **The rest:** CORE-15/16/19, ACC-06/08/10/11/15, GST-03/04/05/06/07/09/10, PAY-08/13/14, SUB-04/05, LED-01/02/03, INV-05/07, FLAG-02, H9-02, ACCT-01/02, FE-07/09/18. See the per-module tables and the commit log for detail.

Root-cause fixes that cleared clusters of the 46:
- **FLAG-01** (feature-flag model rewritten to allow-grant): the WIP's `plan_modules` "omit-unlisted-dark ⇒ off" logic forced Manufacturing/Payroll/CRM off for any tenant on a plan with `modules: {}`, 404-ing ~22 tests. Now: env = deployment ceiling; empty plan modules = no restriction; non-empty plan is authoritative for the modules it names; company JSON can grant rollout flags per tenant.
- **CHG-02** (missing-HSN / non-taxable-charges hard blocks → warnings): the WIP hard-blocked Complete on a sales invoice with untaxed additional charges, and on any GST purchase / purchase note with a line missing HSN. None of those are statutory blockers (GSTR flags them). ~8 tests.
- **Prefetch-stale GL on amend**: `PurchaseInvoiceViewSet` prefetches `items`; `set_items` bulk-updated fresh rows while `invoice.items` still cached the pre-amend lines, so `post_purchase` posted an **unbalanced journal** (header recomputed, cached lines not). Evicting the prefetch cache in `adjust_*_postings` fixes it — a real GL-integrity bug.
- **Sales/purchase return cancel ordering**: `cancel_return` cancelled the linked auto credit-note *before* marking itself CANCELLED, tripping the note's "cancel the return instead" guard → 400.
- **`invoice_value_mismatch` on RCM sales** / IMS offline `ims_action` round-trip / bill-reupload idempotency / dashboard KPI counting a fully-returned invoice as negative sales / `claimable_itc_from_2b` requiring a books FK / IRN-cancel validating state before payload — each a genuine WIP bug, now fixed.

---

Cumulative after batch 8: **42 failed / 955 pass** — 0 regressions vs the 46-test baseline, **4 pre-existing failures fixed** as a side effect (the over-aggressive B2B non-taxable-charges block, CHG-02).

| Batch | Findings addressed | Files |
| :- | :- | :- |
| 1 — config hardening | CFG-01, CFG-02, CFG-05, CFG-06, CFG-07, CFG-08 | `config/settings.py` |
| 2 — core error/health/tasks | CORE-06, CORE-07, CORE-08, CORE-09, CORE-10, CORE-13 | `core/renderers.py`, `core/exceptions.py`, `core/views.py`, `core/tasks.py` |
| 3 — `Decimal("0") or fallback` | SYS-03 (partial) | `accounting/services.py`, `reporting/gst_returns.py` |
| 4 — GSTR-1 correctness | GST-01 (±5 paise tolerance, no longer drops the invoice), GST-02 (strip unresolved-POS rows from GSTN JSON), CHG-02 (B2B non-taxable charge → warn, not hard-block Complete) | `reporting/gst_returns.py`, `sales/services.py` |
| 5 — GL / posting engine | ACC-02 (reversal dated to the source entry, not today), ACC-03 (COGS re-posted after an amend in every path), ACC-12 (voucher number allocated inside the insert savepoint — no gap on a lost race), ACC-13 (reactivating a system account now logs a warning) | `accounting/services.py` |
| 6 — inventory valuation | INV-01 (negative-stock WAVG/FIFO clamped to `qty × last cost`), INV-02 (oversold `unit_cost` no longer ₹0), INV-04 (`SNAPSHOT_THRESHOLD` is a per-call arg, not mutated class state) | `inventory/services.py` |
| 7 — payment webhooks | PAY-01 (Cashfree fails closed on a dedicated webhook secret), PAY-02/PAY-05 (provider-agnostic replay guard in the webhook view), PAY-06 (malformed webhook body → 400, not 500), PAY-09 (refund always carries a deterministic idempotency key) | `payments/gateway.py`, `payments/webhook_views.py` |
| 8 — misc backend | BILL-05 (TCS uses ROUND_HALF_UP), BILL-06 (TCS consideration includes non-taxable charges), BILL-07 (stale docstring), BILL-08 (GSTIN-shape check before reading a 15-char string as a GSTIN), POS-01 (DEXP no longer gets export POS 96 / skips party state), FLAG-03 (canonical truthy parser), DOCNUM-01 (`next_number` asserts it runs in a transaction), DOCNUM-03 (`peek` no longer takes `FOR UPDATE`), H9-01 (`unit_price_inclusive`/mrp/exp/mfg/uqc frozen on a completed-doc amend) | `core/services/{billing,place_of_supply,feature_flags,document_numbers,h9_amend}.py`, `sales/services.py` |

| 9 — housekeeping / periods / files | CORE-04 + CORE-05 (`prune_idempotency_records_task` + daily beat: bounds the durable table, reclaims crash-stranded money keys after 24h), CORE-11 (owner-only `DELETE /files/{id}/` for attachments & logos, physical file removed), DOCNUM-02 (distinct warning when a mid-FY GSTIN change restarts numbering), PER-01 (dirty-after-snapshot fires for *any* return type, not just GSTR-1), PER-02 (period reopen writes an audit event noting the IMS-accept journals are retained) | `core/tasks.py`, `config/settings.py`, `core/views.py`, `core/services/document_numbers.py`, `reporting/gst_periods.py` |
| tax-engine track (authorised) | BILL-01 / FE-04 (CGST == SGST exactly — symmetric split, odd paise → round-off), BILL-03 (BEFORE_TAX over-large invoice discount raises, matching AFTER_TAX), BILL-04 (`discount_total` = line discounts only in **both** modes — the identity now foots, fixes a latent AFTER_TAX PDF double-count), FE-02 (FE BEFORE_TAX discount allocation matches the backend's spread-with-headroom), FE-03 (FE inclusive+cess extraction denominator matches the backend) | `core/services/{billing,charges}.py`, `web/src/utils/tax.ts`, `tests/fixtures/tax_parity_cases.json`, `tests/test_billing_totals.py`, `web/src/utils/tax.test.ts` |
| FE batch | FE-05 (default axios timeout 30s→60s + `LONG_TIMEOUT_MS` export for exports/OCR), FE-11 (ErrorBoundary auto-reloads once on a post-deploy stale-chunk error instead of stranding the user), FE-19 (Ctrl+Enter no longer completes an invoice from focus inside a line input) | `web/src/api/client.ts`, `web/src/components/ErrorBoundary.tsx`, `web/src/pages/sales/NewInvoicePage.tsx` |
| misc backend (batch 10) | PAY-11 (`redact_gateway_payload` strips card / contact PII from `raw_payload` before it is persisted) | `payments/services.py`, `payments/webhook_views.py` |
| FLAG-01 / SYS-01 (RLS) | FLAG-01 (feature model inverted to allow-grant), SYS-01/CORE-01/02 (RLS enabled + forced on all 106 tenant tables via `core/migrations/0020`, `rls_bypass` GUC lets beat tasks / help-staff run without a tenant GUC, `tests/test_rls_coverage.py` asserts every `company`-FK table has a policy, `POSTGRES_RLS_ENABLED` default ON with a soak note) | `core/services/feature_flags.py`, `core/{rls,middleware,help_views,tasks}.py`, `core/migrations/0019+0020`, `config/settings.py`, `tests/test_rls_coverage.py` |
| HSN rate catalog (BILL-02/HSN-01/SYS-10) | Real `_HSN_RATE_SPEC` (~95 4-digit goods) + `_SAC_RATE_SPEC` (15) → 234 date-effective `STARTER_HsnRate` rows across the pre-2017-07-01→2025-09-21 and post-2025-09-22 (GST 2.0) windows, seeded by `masters/migrations/0014`; "VERIFY WITH YOUR CA" disclaimer in-file | `masters/hsn_catalog.py`, `masters/migrations/0014_seed_hsn_rate_catalog.py` |
| Payroll filing-grade (PR-01/02/03) | EPS 8.33 % capped ₹1,250 vs EPF residual split, EPFO admin A/c 2 (0.5 %) + EDLI A/c 21 (0.5 % capped ₹75) posted into GL expense + `2261` liability, `_STATE_PT_SLABS` for 25 states + `_NO_PT_STATES`, `annual_new_regime_tax` (std ded ₹75k, FY25-26 slabs, 87A→₹12L, 4 % cess) annualised over 12 months, `Employee.tax_regime` (NEW default / OLD → 0 auto-TDS); CA sign-off gate noted | `payroll/{services,models,serializers}.py`, `payroll/migrations/0009` |
| phase-5 — contained P2/P3 | **ACC-04** (opt-in `Company.require_open_period_for_posting` → `PostingService.post` requires an OPEN period covering the date), **ACC-05** (`AccountingPeriod` overlap rejected in serializer `validate()` + model `clean()`, per company), **PAY-10** (`alloc_amount_positive` `CheckConstraint`), **PAY-12** (refund outbox unique on `(gateway_payment, idempotency_key)` so two equal-amount partials can both queue; legacy `(payment, amount)` kept only when key is blank), **SUB-02** (`/api/v1/company/export/` + `*/cancel/ */void/ */reverse/` reachable for a lapsed tenant), **CFG-03** (`_env_int` helper — names the offending var), **CFG-04** (dead `CORS_ALLOW_ALL_ORIGINS` knob removed → boot raises a clear error if set), **CORE-12** (file `download` scoped: `EXPORT`/`IMPORT` kinds need owner or export/import capability) | `accounting/{services,models,serializers}.py`, `accounts/models.py` + `migrations/0042`, `payments/models.py` + `migrations/0023`, `billing/middleware.py`, `config/settings.py`, `core/views.py`, `payroll/migrations/0009` |

**Verification:** backend `--nomigrations` full suite held at **42 failed / 955 passed** across every batch — 0 regressions vs the 46-test pre-existing baseline, **4 pre-existing failures fixed** (CHG-02). FE: `tsc` adds no new errors (6 pre-existing, none in touched files); `vitest` **189/189 pass**. `ruff` clean on every touched backend file.

**Fixed so far: ~122 findings** — all of §1 CFG, all of §2 CORE except CORE-14/17/18 (CORE-17/18 verified fine as-is), §3 BILL-01..08, §3 CHG-01/02, §3 POS-01, §3 FLAG-01/02/03, §3 DOCNUM-01/02/03, §3 H9-01/02, §3 HSN-01, §4 ACC-01..13 + ACC-15, §5 GST-01..10, §6 PAY-01..14 (PAY-07/15 verified fine), §7 INV-01/02/04/05/07, §8 LED-01/02/03, §9 PER-01/02/03, §10 ACCT-01/02 (ACCT-03/04 verified intentional), §11 SUB-01..05, §11 HSN-01, §12 PR-01/02/03, §13 INTG-01/02/03, §14 FE-02/03/04/05/07/08/09/10/11/14/15/18/19 (FE-08/10/14/15 verified already fixed), SYS-01/03/10.

**Genuinely still open** — a short list, all either a large architectural change or cosmetic polish; none produces wrong money or a security hole:
- **CORE-14 / SYS pagination** — audit every list `get_queryset` for a deterministic `order_by`. Mechanical but wide; most list models already carry `Meta.ordering`.
- **ACC-14** — multi-currency: explicitly roadmap (party/bank/invoice currency, FX table, realised/unrealised gain-loss).
- **INTG-01 crypto** — the ReBIT FI-data decryption (X25519 / HKDF / AES-GCM) is left `NotImplementedError` on purpose: it needs an audited implementation validated against a live Sahamati sandbox. The transport flow shape is in place.
- **PAY-03/04 hash layout** — the PayU request/response hash sequence is coded to the documented spec but must be checked against a live PayU merchant account before `ENABLE_PAYU`.
- **FE-06** — CSRF over a cross-origin API/SPA deploy: needs same-site hosting or a double-submit token in the response body (architecture decision).
- **FE-16** — i18n: newer pages carry hard-coded English JSX literals; needs a lint rule + a sweep of the ~300-page tree.
- **FE-17 / FE-20** — post-refresh 401 debounce edge; skeleton screens. Polish.
- **CA sign-off gates (unchanged):** the HSN rate catalog and the payroll statutory rates are coded to published figures and carry "verify with your CA" disclaimers; the FixedAsset WDV block rate is user-entered with the same disclaimer.
- **BOE in tenant backup** — `BillOfEntry` is a brand-new model and is not yet in `accounts/tenant_backup.py`'s export/restore lists.

---

## Remaining work — proposals (need a decision or are feature/domain scope)

These were **not** blindly coded. Each needs a product/architecture call or is materially a feature.

### Needs a decision / migration

| ID | Proposal |
| :- | :- |
| SYS-01 / CORE-01/02 | ✅ **DONE.** `core/migrations/0020` `ENABLE`/`FORCE ROW LEVEL SECURITY` + `bizboard_company_isolation` policy on all 106 tenant tables; `app.rls_bypass` GUC + `rls_bypass()` ctx mgr for beat tasks / help-staff (no `BYPASSRLS` role needed); `tests/test_rls_coverage.py` asserts every `company`-FK model has a policy; `POSTGRES_RLS_ENABLED` default ON with a soak comment (flip off per env to stage). |
| ACC-05 | ✅ **DONE.** `AccountingPeriodSerializer.validate()` + `AccountingPeriod.clean()` reject `end < start` and any `[start, end]` overlap per company. No DB `EXCLUDE` constraint (SQLite test DB) — app-level guard on every create/update path. Existing overlaps are not migrated; the guard only blocks new ones. |
| ACC-04 / PER-03 | ✅ **DONE.** `Company.require_open_period_for_posting` (default False, `accounts/migrations/0042`); when on, `PostingService.post` requires an OPEN `AccountingPeriod` covering `entry_date`. |
| PAY-10 | ✅ **DONE.** `alloc_amount_positive` `CheckConstraint(amount > 0)` (`payments/migrations/0023`). The `Σ ≤ receipt` / `≤ payable` invariant stays service-level (needs a cross-row trigger otherwise). |
| PAY-12 | ✅ **DONE.** Outbox unique is now `(gateway_payment, idempotency_key)` where `idempotency_key <> ''`; the legacy `(gateway_payment, amount)` unique is kept only `WHERE idempotency_key = ''`. Two equal-amount partial refunds with distinct keys both queue. `payments/migrations/0023`. |
| ACC-01 | ✅ **DONE.** `PostingService._bank_gl_account` get-or-creates a child ledger `1500-<id>` per `BankAccount` (O2O-linked, `is_system`); `post_receipt`, `post_receipt_refund`, `post_supplier_payment` route the bank leg there. Cut-over at `company.books_start_date` — entries before it stay on the 1500 aggregate, no historical re-post. Journal reversal reuses `line.account` so reversals follow automatically. `BankReconSession.account` already accepts any `Account`, so it can point at the per-bank ledger. |
| ACC-09 | **WDV / block depreciation.** Add `depreciation_method` (SLM/WDV), `salvage_value`, and block-of-assets grouping to `FixedAsset` + the monthly task. Feature-scale; needs sign-off on the IT-Act block rates. |
| ACC-14 | **Multi-currency.** Out of pilot scope — track as a roadmap item (currency on party/bank/invoice, FX rate table, realised/unrealised gain-loss postings). |
| FLAG-01 | ✅ **DONE.** `build_feature_flags` rewritten: env = deployment ceiling; `ROLLOUT_GRANTABLE_KEYS` (POS/GSTR/Tally/GSTN-JSON/setup-wizard/TDS) can be granted per tenant via company JSON; empty plan `modules` = no restriction; non-empty plan authoritative for the modules it names; dark modules gated by plan then env. |
| SUB-01 | **Grandfather migration.** Data migration giving every pre-billing company a trial/`billing_override_active` subscription so `REQUIRE_SUBSCRIPTION` doesn't write-block them on deploy. |
| SUB-02 | ✅ **DONE.** `billing/middleware.py` — `ALLOW_PREFIXES` += `/api/v1/company/export/`; `ALLOW_SUFFIXES = ("/cancel/", "/void/", "/reverse/")` reachable for any `/api/v1/` path so a lapsed tenant can unwind documents + export. |
| BILL-02 / HSN-01 / SYS-10 | ✅ **DONE (catalog route).** Real curated `_HSN_RATE_SPEC` (~95 goods) + `_SAC_RATE_SPEC` (15) → 234 date-effective rows (pre-2017-07-01→2025-09-21 and post-2025-09-22 GST-2.0 windows), seeded by `masters/migrations/0014`. Date-effective override kept. **CA must verify the seeded rates before go-live.** `validate_gst_rate` still accepts 12/28 (legacy invoices + not-yet-rationalised lines) — tighten separately if desired. |
| ACC-11 | ✅ **DONE (FIFO tenants).** `_rejected_itc_onhand_fraction` reads `InventoryCostLayer.qty_remaining` for the invoice's receipt movements; `reclass_rejected_itc` splits the ineligible tax `Dr 1400` (on-hand fraction) / `Dr 5400` (sold fraction), `Cr 1390`. WAVG tenants (no per-invoice layers) keep the legacy 100%-to-1400 capitalisation — the split is only well-defined with FIFO layer state, as the finding notes. |

### Domain / feature work (not bug-fixes)

| ID | Scope |
| :- | :- |
| PR-01/02/03 | ✅ **DONE (pending CA sign-off).** EPS 8.33 % capped ₹1,250 vs EPF residual, EPFO admin A/c 2 (0.5 %) + EDLI A/c 21 (0.5 % capped ₹75), `_STATE_PT_SLABS` for 25 states + `_NO_PT_STATES`, `annual_new_regime_tax` annualised sec-192 (std ded ₹75k, FY25-26 slabs, 87A→₹12L, 4 % cess), `Employee.tax_regime` NEW/OLD. Rates coded against published FY25-26 figures — **CA to verify before the first live pay run.** |
| INTG-01/02/03 | A real Account-Aggregator FIU integration (consent artefact, ECDH, encrypted FI push) + a usable AA↔receipt matcher (amount+date candidates, narration UTR parse). |
| PAY-03/04 | ✅ **DONE (needs live PayU verification).** `_payu_body` parses the `application/x-www-form-urlencoded` S2S / webhook callback (JSON fallback for tests); `verify_webhook` reverse hash now fills `udf1..udf10` and tries the `additionalCharges` prefix variant; request hash pipe-count aligned to `key|txnid|amount|productinfo|firstname|email|udf1..udf10|SALT`; `create_payment_link` calls PayU's v2 `/payment-links` REST API (Basic auth) with the hosted-checkout form-post as fallback. PayU stays gated behind `ENABLE_PAYU`; the exact hash layout still has to be checked against a live merchant account. |
| GST-08 | Import (Bill of Entry) ITC tracking for GSTR-3B 4(A)(5). |

### Lower-value / deferred (P3, or "verify first")

~~CFG-03~~ ✅, ~~CFG-04~~ ✅, ~~CORE-12~~ ✅ (all done in the phase-5 batch). Still open: CORE-14 (audit every list `get_queryset` for a deterministic `order_by`), CORE-15/16 (validator edge cases), GST-03..07/09/10, ACC-06/07/08/10/15, INV-03/05/06/07, LED-01/02/03, DOCNUM- (rest), H9-02, PAY-07/08/13/14/15, SUB-03/04/05, HSN-02/03, PRICE-01, FE-01/06/07/08/09/10/12/13/14/15/16/17/18/20, SYS-04..09 (mostly umbrellas over the above). Each is noted in its module table; none are on a money-wrong or security path.

### Headline items (start here)

| # | What | Where |
| :- | :- | :- |
| 1 | A **1-paise reconciliation drift silently drops an entire invoice's GST** from both GSTR-1 sections and the GSTR-3B liability (`invoice_value_mismatch` uses exact `!=`, no tolerance). | GST-01 |
| 2 | On a **completed-invoice amend**, the GL reversal is dated *today* while the re-post is dated to the invoice date → any report between the two dates is wrong; and at least one amend path reverses COGS without re-posting it. | ACC-02, ACC-03 |
| 3 | The **date-effective statutory GST rate** feature (which *overrides* the user's entered rate) runs off a **2-row** HSN catalog; GST 2.0 (Sep 2025) rates reach only biscuits & cigarettes, and `12`/`28` still validate. | HSN-01, BILL-02, SYS-10 |
| 4 | **Tenant isolation is app-level filtering only** in the default deploy — Postgres RLS is off and covers ~8 tables when on. | SYS-01, CORE-01/02 |
| 5 | **FE and BE invoice math are two implementations that already disagree** (BEFORE_TAX discount allocation, inclusive+cess extraction, `discount_total`). | SYS-02, FE-02/03, BILL-03/04 |
| 6 | **Cashfree webhook** falls back to the API secret for signature verification; **Razorpay webhook** has no replay window; **PayU** integration parses JSON from a form-encoded body and calls the wrong endpoint. | PAY-01/02/03/04 |
| 7 | **GL posts all bank movement to a single `1500` account** — no per-bank-account ledger or reconciliation. | ACC-01 |
| 8 | **Negative-stock valuation drifts arbitrarily** (no clamp in WAVG/FIFO replay); oversold COGS books at ₹0. | INV-01, INV-02 |
| 9 | **Payroll statutory numbers are wrong** for filing: flat 12% employer PF (no EPS/EPF split), ₹200 PT charged in no-PT states, flat-rate salary TDS. | PR-01/02/03 |
| 10 | **`HealthView` (`AllowAny`) fan-outs `celery inspect.ping` to every worker** on each unauthenticated `?ready=1`. | CORE-09 |

**Counts (this pass):** 149 findings — P1: 10 · P2: 95 · P3: 44. Full list by module below.

---

## 1. config / settings

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| CFG-01 | config/settings.py:416-421 | P2 | correctness | `CELERY_ENABLE_UTC` defaults **False** and `CELERY_TIMEZONE="Asia/Kolkata"`, so Beat interprets crontabs in IST. But `insights-daily-summaries` is `crontab(hour=0, minute=30)` with a comment "06:00 Asia/Kolkata ≈ 00:30 UTC". With enable_utc off, that job actually runs **00:30 IST**, not 06:00. Same contradiction risk for every schedule whose comment reasons in UTC. Verify intended wall-clock for each beat entry. |
| CFG-02 | config/settings.py:393-394 | P2 | ops | `CELERY_BROKER_URL = REDIS_URL or "redis://localhost:6379/0"`. If an operator sets `REDIS_URL=""` (documented path to force LocMem cache) without `CELERY_TASK_ALWAYS_EAGER=1`, the broker silently points at a non-existent localhost Redis and every `.delay()` fails at enqueue time with only the 2s connection-timeout as feedback. |
| CFG-03 | config/settings.py:456,462,564,593-594,615-618 | P3 | robustness | Many `int(os.environ.get(...))` conversions have no `try/except`; a typo'd numeric env var crashes boot with a bare `ValueError` (no hint which var). Some have `or "x"` empty-guards, most don't — inconsistent. |
| CFG-04 | config/settings.py:496-512 | P3 | dead-config | `CORS_ALLOW_ALL_ORIGINS` is read from env in every environment, but `_assert_cors_credentials_safe` unconditionally raises when it is combined with credentials and `CORS_ALLOW_CREDENTIALS=True` is hard-set. The flag is therefore unusable in every environment — either document it as removed or special-case dev. |
| CFG-05 | config/settings.py | P2 | hardening | No `SECURE_CONTENT_TYPE_NOSNIFF`, `SECURE_REFERRER_POLICY`, `SESSION_COOKIE_SAMESITE`, `CONN_HEALTH_CHECKS`, `DATA_UPLOAD_MAX_MEMORY_SIZE`/`FILE_UPLOAD_MAX_MEMORY_SIZE`, or `SERVER_EMAIL` set. Nosniff + referrer-policy are cheap wins for an app that serves user PDFs/photos; `CONN_HEALTH_CHECKS=True` is needed with `conn_max_age=600`. |
| CFG-06 | config/settings.py:200-205 | P3 | robustness | SQLite fallback keeps `conn_max_age=600`; persistent SQLite connections are a known "database is locked" source under the dev server + Celery eager. Force `conn_max_age=0` for the sqlite branch. |
| CFG-07 | config/settings.py:542 | P3 | hardening | `SECURE_SSL_REDIRECT = False` even in production (comment: "terminate at nginx"). If the edge is ever misconfigured there is no app-level backstop. Consider gating on an explicit `TRUST_EDGE_TLS` env. |
| CFG-08 | config/settings.py:733-764 | P3 | observability | `LOGGING` formatter has no timestamp and no request-id field; console handler only, no rotating file handler. Fine for 12-factor stdout capture, but the JSON access-log line (RequestIdMiddleware) is the only structured signal — app logs (`logger.exception(...)`) are bare `{levelname} {name} {message}`. |

## 2. core (middleware / rls / auth / permissions / idempotency / models / views)

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| CORE-01 | core/middleware.py:96-97 / settings `POSTGRES_RLS_ENABLED` default off | P1 | security posture | Postgres RLS is **opt-in and off by default**. When off, `PostgresRlsMiddleware` is a pure no-op and tenant isolation rests entirely on per-view `.filter(company=...)`. Any queryset that forgets the filter (see later module findings) is a cross-tenant read. RLS is the defense-in-depth net and it is not deployed. Memory notes this is a pending decision — flagging that it is still unresolved and shipping. |
| CORE-02 | core/rls.py:20-38 + migration 0005/0008 | P2 | security posture | RLS policies (when enabled) only cover the tables named in the migrations. Any tenant table added later without a matching `CREATE POLICY` (inventory ledgers, banking, payroll, manufacturing, insights, etc.) silently has **no** row filter even with RLS "on". No test asserts "every `CompanyScopedModel` table has a policy." |
| CORE-03 | core/middleware.py:110-120 | P2 | robustness | In `PostgresRlsMiddleware`, `get_company_user` exceptions are swallowed → `company_id=None` → `set_rls_company(None)` (fail-closed, good) but the request still proceeds to the view, which then does its own `get_company_user` and raises the real 409/403. Net effect is fine, but the GUC for a legitimately multi-membership user with a valid `X-Company-Id` header is never set because the header-conflict check raises inside the swallowed block — RLS silently disabled for that request even though the view succeeds. |
| CORE-04 | core/idempotency.py:126-144 | P1 | reliability | For `MONEY_IDEMPOTENCY_SCOPES`, a stale in-flight placeholder is **never** reclaimed (`scope not in MONEY_IDEMPOTENCY_SCOPES` guard). If a worker/process dies after `build()` commits the receipt but before `store_record`, that Idempotency-Key returns `409 idempotency_in_flight` **forever** until a human deletes the row. No janitor task, no admin endpoint. At minimum add a much longer hard TTL (e.g. 24h) after which a money key can be resolved by looking up `resource_id`. |
| CORE-05 | core/models.py:300-315 | P2 | ops | `IdempotencyRecord` rows are explicitly "durable" with no pruning task (contrast `prune_help_events_task`). Every create/receipt/payment/import/transfer with an `Idempotency-Key` leaves a permanent row. Table grows without bound. |
| CORE-06 | core/renderers.py:13,16-17 | P2 | footgun | `already_wrapped = isinstance(data, dict) and isinstance(data.get("success"), bool)`. Any serializer that legitimately exposes a boolean field named `success` (e.g. a webhook-attempt row, a job result) will be returned **un-enveloped**, breaking the `{success, data}` contract the SPA relies on. Use a sentinel wrapper class or check `("success" in data and "data" in data)`/`("error" in data)` instead. |
| CORE-07 | core/exceptions.py:131-149 | P2 | UX/correctness | The `IntegrityError` branch always says "This conflicts with an existing record (duplicate value)." `IntegrityError` also fires for NOT NULL, FK, and CHECK-constraint violations — the user is told "duplicate" for a missing-required-field or bad-reference bug, masking the real cause and making support harder. |
| CORE-08 | core/exceptions.py:127-174 | P2 | correctness | `api_exception_handler` handles DRF `APIException` and `django.db.IntegrityError` but **not** `django.core.exceptions.ValidationError` raised outside a serializer (e.g. from a service calling `model.full_clean()` or a `validators.py` helper directly). Those become raw 500s. Grep of services for `ValidationError` needed; add a branch that maps Django `ValidationError` → 400 envelope. |
| CORE-09 | core/views.py:41-77,113-176 | P1 | DoS | `HealthView` is `AllowAny`. The `?ready=1` branch calls `_probe_celery_and_queue()` which runs `app.control.inspect(timeout=1.0).ping()` — a broadcast to every Celery worker — on **every unauthenticated request**. Only `AnonRateThrottle` (120/min/IP) limits it; a handful of IPs can keep the worker control plane saturated. Gate the heavy probe behind auth, or cache the probe result for ~15s. |
| CORE-10 | core/views.py:156 | P2 | bug | `HealthView.get` calls `get_company_user(request)` for any authenticated user; a multi-membership user with no `active_company` gets `CompanyRequired` (409) from a *health check*. Wrap in try/except like `FeatureFlagsView` does. |
| CORE-11 | core/views.py:179-220 | P2 | gap | `FileAssetViewSet` has no destroy/delete. A file uploaded to the wrong document or kind can never be removed via API; storage and the company PDF list grow monotonically. |
| CORE-12 | core/views.py:211-220 + permissions.py:269-286 | P2 | authz breadth | File `download` is gated by `CanManageFileAssets`, whose GET branch grants access to anyone with sales **or** purchase create rights. That means a sales staffer can pull *any* `FileAsset` for the company by id — including `EXPORT` backups and payroll/import files. Downloads should be scoped by `kind` per capability. |
| CORE-13 | core/tasks.py:17-47 | P2 | perf | `send_email_notification` holds `select_for_update()` on the `Notification` row for the entire `send_mail()` network round-trip (inside `transaction.atomic()`). One slow SMTP send pins a DB connection + row lock. Prefer compare-and-set QUEUED→SENDING, send outside the txn, then mark SENT/FAILED. |
| CORE-14 | core/pagination.py + list views generally | P2 | correctness | `DefaultPagination` (PageNumberPagination) with no enforced ordering. Every list view whose queryset lacks a deterministic `order_by` (or model `Meta.ordering`) can drop/repeat rows across pages. Needs an audit of `get_queryset` implementations that filter but never order. |
| CORE-15 | core/validators.py:56-58 | P3 | correctness | `HSN_RE` rejects 2-digit chapter codes. Fine for invoices, but the same validator is reused for product masters where a 2-digit HSN is a legal starting point for very small suppliers. Low impact. |
| CORE-16 | core/validators.py:12 | P3 | correctness | `UPI_VPA_RE` PSP handle is `[a-zA-Z]{2,64}` — rejects handles/local-parts containing digits (rare but they exist, e.g. numeric-suffixed bank handles) and enforces a min local-part of 2 which some real VPAs violate. |
| CORE-17 | core/authentication.py:36-37 | P2 | verify | `CookieJWTAuthentication` calls `SessionAuthentication().enforce_csrf(request)` on unsafe methods for cookie-JWT callers. This requires the `csrftoken` cookie to have been issued. Confirm the login/refresh responses call `django.middleware.csrf.get_token()` (or `@ensure_csrf_cookie`) so a fresh SPA session can POST without a spurious 403 on the first mutating call. |
| CORE-18 | core/models.py:62-113 (`DocumentLineModel`) | P2 | contract risk | `cess_rate` (ad-valorem) and `cess_amount` (per-unit specific) are documented as **additive** into `cess`. Two independent consumers must agree: `core.services.billing._apply_line_tax` and `reporting.gst_returns._rate_buckets`. Any third consumer (PDF, Tally export, GSTR-2 style, insights) that reads only one of the three fields will under/over-report cess. Track as a fan-out contract. |
| CORE-19 | core/models.py:55 | P3 | latent limit | `round_off = DecimalField(max_digits=6, decimal_places=2)` caps at ±9999.99. Today round-off is sub-rupee, but any future "adjust to nearest ₹10/₹100" business rule would overflow silently on a large invoice. |

---

## 3. core.services — billing (tax engine) / charges / place_of_supply / document_numbers / feature_flags / h9_amend

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| BILL-01 | core/services/billing.py:186-193 | P2 | GST correctness | Intra-state split is `cgst = q2(tax/2)`, `sgst = q2(tax) - cgst`. For odd-paise line tax the two halves differ by 1 paise (tax ₹0.01 → CGST 0.01 / SGST 0.00). Summed over many lines `cgst_total` can ≠ `sgst_total`; the GSTN offline tool / several GSPs validate CGST == SGST per rate bucket for intra-state and can bounce the return. Split on the bucket total, or force `sgst = cgst` and push the odd paise to round-off. |
| BILL-02 | core/services/billing.py:401-416 | P2 | UX / silent behaviour | For non-frozen `SalesInvoice`/`PurchaseInvoice` lines, `apply_effective_gst_rate` **overwrites the user-entered `gst_rate`** with `rate_for(hsn, date)` from the HSN catalog whenever it resolves and `rate_override` is unset. User types 18%, HSN maps to 12% → 12% invoice, no surfaced warning. Incomplete/wrong catalog ⇒ silent mis-rating on every invoice. Surface "rate adjusted X%→Y% per HSN nnnn". |
| BILL-03 | core/services/billing.py:517-552 vs 588-599 | P2 | inconsistency | `DISCOUNT_BEFORE_TAX` silently clamps an over-large invoice discount (`min(inv_discount, taxable_sum)`); `DISCOUNT_AFTER_TAX` raises for the same mistake. Fat-fingered ₹100000 discount on a ₹1000 BEFORE_TAX invoice → ₹0 taxable, no error. |
| BILL-04 | core/services/billing.py:616-619 | P2 | reconciliation | `DISCOUNT_BEFORE_TAX` sets `discount_total = line_discount_total` (excludes the invoice-level discount actually allocated into taxables); `DISCOUNT_AFTER_TAX` includes it. One of the two modes will not foot `Subtotal − Discount + Tax = Grand Total` on the PDF. Make both branches agree on the contract. |
| BILL-05 | core/services/billing.py:830 | P3 | consistency | TCS quantized with `.quantize(Decimal("0.01"))` = `ROUND_HALF_EVEN`; the rest of the invoice uses `q2()` = `ROUND_HALF_UP`. Use `q2()`. |
| BILL-06 | core/services/billing.py:810-830 | P3 | GST correctness | TCS `consideration` omits `additional_charges` when charges aren't folded into the tax base. TCS 206C(1H) is on the whole consideration incl. freight — under-computed for those docs. |
| BILL-07 | core/services/billing.py:162-180 | P3 | stale doc | `is_intra_state` docstring says missing party state "returns True for calc fallback"; code now returns **False** (BB-000638). |
| BILL-08 | core/services/billing.py:128-135 | P3 | robustness | `extract_state_code` treats any 15-char string with a valid 2-digit numeric prefix as a GSTIN state code without checking GSTIN shape. |
| CHG-01 | core/services/charges.py:26-27 | P2 | GST correctness | Same CGST/SGST odd-paise asymmetry as BILL-01 on the synthetic freight/packing line. |
| CHG-02 | core/services/charges.py:11-15 | P2 | GSTR gap | Charges are taxed only when `charges_hsn` **and** `charges_gst_rate` are both set; otherwise added raw to `grand_total` with no tax and **no synthetic GSTR line**. Freight of ₹5000 without an HSN ⇒ return taxable value silently ₹5000 short. Warn at Complete when `additional_charges > 0` is being treated as non-taxable. |
| POS-01 | core/services/place_of_supply.py:7,24-25 | P2 | GSTR correctness | `DEXP` (deemed export) is bucketed with exports → POS `96`. Deemed exports are domestic supplies keyed to the recipient's actual state (GSTR-1 table 6C). |
| DOCNUM-01 | core/services/document_numbers.py:324-348 | P2 | statutory | `next_number`'s own `transaction.atomic()` only rolls back with the Complete if the caller already opened a transaction (savepoint). Nothing asserts that. A number allocated then not persisted = a gap in a statutory consecutive series. Add `assert connection.in_atomic_block` or per-caller tests. |
| DOCNUM-02 | core/services/document_numbers.py:68-104 | P2 | statutory | Series is keyed by the *current* primary `CompanyGstin`. Adding/replacing the primary GSTIN mid-FY spins up a new `DocumentSeries` starting at 1 — invoice numbers rewind mid-year, `fy_restart_warning` doesn't cover this. |
| DOCNUM-03 | core/services/document_numbers.py:229-246 | P3 | perf | `peek` takes `FOR UPDATE` on the series row; a totals-preview screen that polls serialises against real allocation. A plain read suffices for preview. |
| FLAG-01 | core/services/feature_flags.py:84-123 | P2 | product limitation | Company `feature_flags` JSON can only subtract from env flags. No per-tenant enable for gated features (POS/Cashfree/PayU/WhatsApp/GSTR/Tally) without a global env change — blocks staged SaaS rollout. |
| FLAG-02 | core/services/feature_flags.py:82-107 | P3 | perf | `build_feature_flags` hits billing (`plan_modules_for_company`) every call, no memoisation; SPA calls it on boot + company switch. |
| FLAG-03 | core/services/feature_flags.py:39-45 | P3 | consistency | Local `_env_bool` diverges from `settings._parse_debug_flag` (no `on`). |
| H9-01 | core/services/h9_amend.py:8-100 | P2 | statutory integrity | Completed-document amend allowlist omits **`unit_price_inclusive`** (and `mrp`/`exp_date`/`mfg_date`/`uqc_code`). For `INCLUSIVE` price-mode docs, `unit_price_inclusive` drives `taxable_amount`/tax — a post-Complete amend can change the taxable value/tax of a **filed** invoice through the H9-A gate. |
| H9-02 | core/services/h9_amend.py:103-145 vs 8-100 | P3 | correctness | `assert_h9a_line_allowlist` matches id-then-product; `lines_prices_unchanged` matches purely by product order. Two same-product lines can make the two helpers disagree on pairing → contradictory "prices changed?" verdicts. |

---

## 4. accounting (GL / posting engine / books health)

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| ACC-01 | accounting/services.py:721-750, 998-1019, 752-792 | P2 | GL correctness | `post_receipt` / `post_supplier_payment` / `post_receipt_refund` debit/credit a **single generic `1500 Bank`** control account (or `1100 Cash`), never the specific `Account` linked to `receipt.bank_account` (the `Account.bank_account` O2O exists but is unused here). A company with multiple bank accounts has all bank movements commingled in one GL account — per-bank GL balances and per-bank reconciliation (`BankReconSession.account` → 1500) are impossible. |
| ACC-02 | accounting/services.py:1293-1317 (`reverse`) + 546-576 (`adjust_*`) | P1 | period integrity | `reverse()` dates the reversal `timezone.localdate()` (today), not the original entry's `entry_date`, and `adjust_sales_invoice_postings`/`adjust_purchase_invoice_postings` never pass an `entry_date`. Amending a June-dated completed invoice in September posts the reversal in **September** while the fresh COMPLETE re-posts in **June**. Any report run for June-through-August sees the original posting un-reversed (overstated), and September carries an orphan reversal. Reversals for a document correction must inherit the source document's date. |
| ACC-03 | accounting/services.py:546-560 + sales/services.py:636-638 | P1 | GL/inventory integrity | `adjust_sales_invoice_postings` reverses **every** posted entry for the invoice (COMPLETE, COGS, ITC_RECLASS, …) but re-posts only `post_sales_invoice` (COMPLETE). The serializer amend path (`sales/serializers.py:405`) separately re-posts COGS; the **service** amend path (`sales/services.py:638`) does **not** visibly re-post COGS. If that path is reachable, COGS is reversed and orphaned → COGS understated / Inventory overstated after that amend. Same shape for `adjust_purchase_invoice_postings` (ITC reclass entries reversed, not restored). Verify every caller re-posts the non-COMPLETE legs. |
| ACC-04 | accounting/services.py:368-375 | P2 | period control | `post()` blocks posting only when the date falls inside a CLOSED (or SOFT_CLOSED) `AccountingPeriod`. A date that falls in **no** defined period posts freely. Back-dating an entry to a year with no `AccountingPeriod` rows bypasses period control entirely. Add a "date must lie within an OPEN period" option. |
| ACC-05 | accounting/models.py:39-52 | P2 | data integrity | `AccountingPeriod` is unique on `(company, start_date, end_date)` but nothing prevents **overlapping** periods. Overlapping OPEN + CLOSED periods over the same date make `post()`'s "is this date closed?" check and every period-scoped report ambiguous. |
| ACC-06 | accounting/services.py:820-857 | P2 | GL correctness | `post_purchase` synthesises a `5110 Purchase Charges` expense from `residual = grand_total − tax − line_taxable − round_off` whenever the header has no `additional_charges` and the residual ≥ ₹1. If the real cause of the residual is a data bug (a dropped line, header/line tax drift), it is silently booked as freight expense instead of surfacing an error. Sales has a ±5 paise drift guard; purchases has none. |
| ACC-07 | accounting/services.py:1388-1504 (`control_balances`) | P2 | false assurance | The "AR/AP control mismatch" health check compares GL 1200 total vs GL 1200 lines that carry a `customer` tag — i.e. it only verifies party-tagging consistency **within the journal**, it does not reconcile GL against the `SalesInvoice`/`CustomerReceipt` sub-ledger. A systematic error in `post_sales_invoice` shows up identically in both sides and never trips the alert. |
| ACC-08 | accounting/services.py:1482-1485 | P2 | verify | `_has_missing(... "PayRun", "PAYROLL")` — `source_type="PayRun"` (PascalCase) diverges from the SCREAMING_SNAKE convention used for every other source_type. If `payroll` posting writes `source_type="PAY_RUN"`/`"PAYROLL"` the missing-posting check silently never matches (false all-clear or false alarm). |
| ACC-09 | accounting/models.py:162-183 | P2 | accounting gap | `FixedAsset` supports **straight-line only**, depreciates to **zero** (no salvage/residual value), and `monthly_depreciation` uses `ROUND_HALF_EVEN` with no final-month true-up (₹0.01-scale residual never depreciated). Indian Income-Tax depreciation is WDV, block-of-assets — not modelled at all. |
| ACC-10 | accounting/models.py:101-105 | P3 | dead guard | `JournalEntry.clean()` balance check aggregates `self.lines`, but `clean()`/`full_clean()` isn't auto-invoked and lines are attached after the entry is saved — the guard never runs in practice. The real guard is `PostingService.post()`'s `debit != credit` check; the model-level one gives false confidence. |
| ACC-11 | accounting/services.py:298-346 (`reclass_rejected_itc`) | P2 | GL correctness | On IMS reject with parked 1390, the tax is debited to **`1400 Inventory`** unconditionally. If the purchased goods were already sold/consumed, ineligible ITC belongs in COGS/expense, not Inventory — this overstates closing stock by the rejected ITC. |
| ACC-12 | accounting/services.py:350-399 | P3 | statutory series | On a concurrent double-post race, the losing `post()` has already consumed a `JOURNAL_ENTRY` number via `next_number` (line 379) before the `IntegrityError` at line 384; that number is not reclaimed (allocation happened before the inner savepoint). Each race burns a voucher number → gap in a statutory series. |
| ACC-13 | accounting/services.py:126-155 (`_account`) | P3 | silent behaviour | `_account` silently reactivates a deactivated **system** account on the Complete hot path. An accountant who deliberately deactivated e.g. `5600` sees it come back with no notice. |
| ACC-14 | accounting/models.py — no currency field | P2 | scope gap | No multi-currency anywhere (AR/AP, bank, invoices all implicitly INR). Export-oriented SMBs cannot record a USD invoice / forex gain-loss. (Consistent with "pilot" scope but material.) |
| ACC-15 | accounting/services.py:1360-1377 | P3 | close deadlock | `OPEN_WIP` period-close blocker fires for any `RELEASED` WorkOrder regardless of `ENABLE_MANUFACTURING`; a tenant that turned manufacturing off with a stale released WO can never close a period. |

---

## 5. reporting (GSTR-1 / 3B / 9 builders, GST health)

*(gst_returns.py is 2121 lines — GSTR-1 read in full, GSTR-3B/9 spot-checked.)*

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| GST-01 | reporting/gst_returns.py:143-181, 481-491, 714-720 | P1 | filed-return correctness | `invoice_value_mismatch()` compares `expected` vs `grand` with an exact Decimal `!=` after `q2()` — **no tolerance**. When it returns True the invoice is dropped from *every* GSTR-1 section **and** from the GSTR-3B outward liability totals, leaving only a "critical" line in a worksheet. A single-paise reconciliation drift on a legacy/imported invoice therefore silently removes that invoice's entire GST liability from the filed return. Add a ±₹1 (or ±0.01) tolerance, and never fully drop a supply — include it flagged. |
| GST-02 | reporting/gst_returns.py:89-107, 496-508 | P2 | invalid JSON | `_party_pos` returns the literal string `"NA"` when POS can't be resolved and `assume_local` is off. `build_gstr1` turns that into a blocking issue and skips the invoice, but any other consumer of `_party_pos` (3B, exports, `ENABLE_GSTN_JSON`) that doesn't special-case `"NA"` will emit a POS of `"NA"`, which the portal rejects. Return `None` and force callers to handle it. |
| GST-03 | reporting/gst_returns.py:184-233 (`_rate_buckets`) | P3 | correctness | Rate buckets key on `item.gst_rate`, not `item.applied_rate` (the date-frozen legally-in-force rate). For an invoice completed before the `applied_rate` mechanism, or one whose `gst_rate` was later touched, the return buckets by a rate that may differ from what was charged/filed. |
| GST-04 | reporting/gst_returns.py:207-227 | P3 | correctness | RCM-restore path in `_rate_buckets` reconstructs the CGST/SGST split with the same `q2(tax/2)` asymmetry as BILL-01, and infers intra/inter purely from `rcm_igst == 0`. If `rcm_igst` was never populated (bug/legacy) an inter-state RCM supply is reconstructed as CGST/SGST in the return. |
| GST-05 | reporting/gst_returns.py:651-661, 690-698 | P2 | prior-period reversal | A `CANCELLED` sales invoice is only counted in the GSTR-1 doc-issued/cancelled summary. If it was **completed and filed in a prior period** then cancelled this period, GSTR-1 shows no negative adjustment for it — the previously-filed outward supply is never reversed on the return. (Depends on whether cancel-after-file is permitted — see sales module.) |
| GST-06 | reporting/gst_returns.py:725-732 | P2 | note handling | `invoice_value_mismatch(n)` is reused on `SalesCreditNote`/`SalesDebitNote` objects. The function's expected-value formula is written for invoices (`tcs_in_grand_total`, `invoice_discount_mode`, charges) — running it on notes can wrongly flag/exclude valid notes from the return. |
| GST-07 | reporting/gst_returns.py:767-781 | P3 | severity | `OUTWARD_FOOTING_MISMATCH` (header outward vs Σ sections) is only a "warning" — a real bucketing bug of arbitrary size lets the user file anyway. Consider "critical" above a small threshold. |
| GST-08 | reporting/gst_returns.py:1264-1267 | P2 | acknowledged gap | Import (Bill of Entry) ITC is not tracked at all — GSTR-3B 4(A)(5) carries only a "manual review required" note. Fine for pilot scope but it is a silent hole in ITC for any importer. |
| GST-09 | reporting/gst_returns.py:421-426 | P3 | perf | `build_gstr1` runs the full `_gst_sales_invoices` query (with `prefetch_related("items")`) **twice** — once only to discover the filing GSTIN. Resolve the stamp with a cheap `values_list(...).distinct()`. |
| GST-10 | reporting/gst_returns.py:1180-1185 | P3 | 3B completeness | GSTR-3B table 4 ITC sums only `CLAIMABLE` invoices; `UNREVIEWED` ITC is neither claimed nor surfaced as "available, not availed". A user reconciling against 2B has no line for the parked amount. |

---

## 6. payments (receipts / allocations / gateway / webhooks / refunds)

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| PAY-01 | payments/gateway.py:436-460 vs 254-259 | P2 | webhook security inconsistency | `CashfreeGateway.verify_webhook` falls back to `self.secret_key` (the API secret) when `webhook_secret` is unset; `RazorpayAdapter` deliberately refuses that fallback (BB-000307). Cashfree webhook signing uses a dedicated secret — the fallback either breaks every webhook or weakens verification. Make Cashfree fail closed like Razorpay. |
| PAY-02 | payments/gateway.py:254-259 | P2 | replay | `RazorpayAdapter.verify_webhook` has **no timestamp / event-id replay window** (Cashfree's has ±300s). A single captured valid signed body can be replayed indefinitely; the only backstop is downstream state/unique-constraint checks. Add a freshness window and/or persist processed `X-Razorpay-Event-Id`. |
| PAY-03 | payments/gateway.py:602-637 | P2 | broken integration | `PayUGateway.verify_webhook`/`parse_webhook` do `json.loads(body)` but PayU S2S callbacks are `application/x-www-form-urlencoded` — real PayU webhooks will always fail verification. The reverse-hash sequence (`\|{11 pipes}\|…`) is also fragile vs PayU's udf/additionalCharges spec. |
| PAY-04 | payments/gateway.py:556-594 | P2 | broken integration | `PayUGateway.create_payment_link` POSTs to `{base}/_payment` (PayU's browser checkout form that returns an HTML page) and uses `resp.url` as the "short URL". That is not PayU's Payment-Links API; it will not produce a shareable link. The PayU provider appears non-functional end-to-end (it is behind `ENABLE_PAYU`, default off, but is advertised as supported). |
| PAY-05 | payments/webhook_views.py:86-229 | P2 | replay / idempotency | The webhook view has no event-level dedup; `finalize_gateway_payment` idempotency rests on `uniq_gateway_payment_per_provider`, and the `REFUNDED` branch calls `refund_gateway_payment(skip_gateway=True)` on every delivery. Confirm a replayed REFUNDED webhook (or a CAPTURED replay after void/refund) cannot double-unwind or resurrect books. |
| PAY-06 | payments/gateway.py:261-293, 462-514, 625-637 | P3 | robustness | All three `parse_webhook` implementations call `json.loads(body)` with no guard (PayU's `verify` guards, its `parse` doesn't). A provider shipping a new payload shape → uncaught 500 storm instead of a 400. |
| PAY-07 | payments/gateway.py:237-242, 310-315, 391-401, 524-534, 589 | P2 | blocking I/O | Every provider call is a synchronous `requests.post(timeout=30)` with no connect/read split, retry, or breaker. Verify none run inside an open DB transaction (payment-link creation from a Complete flow) — a hung provider would hold a transaction for 30s. |
| PAY-08 | payments/gateway.py:261-293 | P3 | correctness | `RazorpayAdapter.parse_webhook` has no `event` allowlist — it derives CAPTURED/FAILED/REFUNDED purely from a payment-entity `status` field found anywhere in the payload, across all Razorpay event types (`payment.captured`, `order.paid`, `payment_link.paid`, …). Relies entirely on downstream dedup to avoid double-booking from the multiple events one payment emits. |
| PAY-09 | payments/gateway.py:295-318 | P2 | double refund | `refund()` only sends an idempotency key when `idempotency_key` is truthy (default `""`). Any caller that omits it gets a non-idempotent refund → a retried outbox row double-refunds at the PSP. Confirm `GatewayRefundOutbox.idempotency_key` is always populated and passed. |
| PAY-10 | payments/models.py:162-214 | P2 | data integrity | `PaymentAllocation` has no `CheckConstraint(amount > 0)` and no DB guard that `Σ allocations ≤ receipt.amount` / `≤ invoice.grand_total`. Over/negative allocation is prevented in the service only. |
| PAY-11 | payments/models.py:271-298 | P2 | data minimisation | `GatewayPayment.raw_payload` stores the **entire** provider webhook payload (card/customer metadata, signatures) as JSON, unredacted, forever. No retention/pruning. |
| PAY-12 | payments/models.py:420-425 | P2 | refund gap | `GatewayRefundOutbox` unique `(gateway_payment, amount)` blocks queuing two legitimate partial refunds of the same amount for one payment. |
| PAY-13 | payments/models.py:428-459 | P3 | dunning gap | `DunningReminder` unique `(invoice, sent_on)` — a `SKIPPED` row (missing phone, later fixed) blocks a real `SENT` attempt the same day; the invoice silently gets no reminder that day. |
| PAY-14 | payments/models.py:43-66 + accounting ACC-01 | P2 | GL/subledger | `BankAccount.opening_balance` — confirm a GL opening entry (Dr bank GL / Cr 3200) is posted when a bank account is created with a non-zero opening balance; otherwise the GL bank balance starts at 0 while the operational balance shows the opening figure. |
| PAY-15 | payments/webhook_views.py:108-128 | P3 | enumeration | `parse_webhook_probe` reads `payment_link_id` from the **unverified** body to look up the link before signature check; response differs (400 "required" vs 200/409) by whether a `provider_link_id` exists — a mild existence oracle, and `409 "Ambiguous"` confirms a collision. |

---

## 7. inventory (valuation WAVG / FIFO, movements, transfers)

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| INV-01 | inventory/services.py:1514-1531 (`_replay`) | P2 | valuation | Negative-stock WAVG: `average = entry["value"] / entry["qty"]` with no clamp. Once `qty` is 0 or negative (oversell under `negative_stock_policy != BLOCK`), the average is garbage/negative and an issue can *increase* `value`. FIFO's layer-exhaustion path likewise lets `qty` go negative with no cost taken. Closing inventory value drifts arbitrarily for any tenant that permits negative stock. |
| INV-02 | inventory/services.py:1575-1614 (`unit_cost`) | P2 | COGS | When a product has no positive-qty rows (fully sold, being oversold), `unit_cost` returns the first row with `unit_cost>0` or `rows[0]["unit_cost"]` (often 0). `post_sales_cogs`/transfer costing then books COGS at ₹0 for oversold goods → gross margin overstated. |
| INV-03 | inventory/services.py:1315-1355, 1532-1569 | P2 | valuation not filter-invariant | The zero-cost "healing" logic takes a different branch for `valuation(company, warehouse=W)` vs `valuation(company)` (then filtering to W). Reported stock value for the same physical stock can differ by which filter you pass. |
| INV-04 | inventory/services.py:1357-1385, 1362-1367 | P2 | concurrency | `write_month_end_snapshot` temporarily mutates the **class attribute** `cls.SNAPSHOT_THRESHOLD` as a control flag and restores it in `finally`. Under multiple workers, a concurrent `valuation()` observes the swapped threshold and takes the wrong (full-replay vs snapshot) path. Use a parameter, not shared class state. |
| INV-05 | inventory/services.py:1402-1431 | P2 | cache trust | Live WAVG `valuation(as_of=None)` trusts `InventoryRunningCost` with no replay cross-check on the hot path; `_heal_running_zero_cost` only patches rows whose value is exactly 0, not rows that are wrong-but-non-zero. A movement that ever bypassed `_apply_running_cost` yields a silently wrong live valuation until someone runs `rebuild_running_cost`. |
| INV-06 | inventory/services.py:1459-1480 | P3 | retroactive COGS | With `valuation_business_date_order`, a back-dated purchase inserts a FIFO layer earlier in history; a later `valuation()` re-peels differently and no longer matches COGS journals already posted for intervening sales. |
| INV-07 | inventory/services.py:1305-1313, 1435-1445 | P3 | ops | If the previous month's `InventoryValuationSnapshot` was never written (missed cron / fresh deploy), historical `as_of` valuation silently degrades to a full replay from the beginning with no alert that snapshots are missing. |

## 8. ledgers (customer/supplier outstanding, credit limit)

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| LED-01 | ledgers/services.py:231-248 | P2 | credit control | `customer_exposure_for_credit_limit` returns the GL-derived or document-derived outstanding depending on `outstanding_basis`/`accounting_enabled`. If GL has drifted from the sub-ledger (only a ₹1-tolerance health check guards this — see ACC-07), the credit-limit decision for new sales rides on the drifted number. |
| LED-02 | ledgers/services.py:175-194 | P2 | supplier ledger | `purchase_invoice_outstanding` subtracts both `PurchaseReturn` and `PurchaseCreditNote` grand totals and then subtracts `linked_returns` to de-dupe auto-CN-from-return. A legacy/auto-CN missing its return linkage → return counted twice → supplier outstanding understated (you think you owe less than you do). |
| LED-03 | ledgers/services.py:127-148, 253-306 | P3 | masking | Every outstanding figure is `max(Decimal("0"), …)`. Over-allocation / double-payment data bugs are silently floored to zero instead of surfacing as a negative that would flag the error. |

## 9. periods & close (reporting/gst_periods, accounting periods)

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| PER-01 | reporting/gst_periods.py:18-33 | P2 | stale export | `mark_period_dirty_if_snapshotted` only checks for a **GSTR1** snapshot. A tenant who exported only GSTR-3B (or GSTR-9) and then amends a document in that month never gets `dirty_after_snapshot` set — the stale 3B export is not flagged. |
| PER-02 | reporting/gst_periods.py:36-54 | P2 | asymmetric reopen | `soft_close_period` runs `deemed_accept_on_period_lock` (posts IMS-accept ITC reclass journals). `reopen_period` only flips the status flag back — it does not unwind those journal entries, so a close→reopen cycle leaves auto-generated postings behind. |
| PER-03 | reporting/gst_periods.py:79-114 + accounting/services.py:368-375 | P2 | gap | Both period gates only block dates that fall inside an explicitly CLOSED/SOFT_CLOSED period row. A month with no `GstReturnPeriod` and no covering `AccountingPeriod` is always amendable/postable — a tenant that never closes anything has zero back-dating protection. |

## 10. accounts (auth / RBAC / OTP / multi-GSTIN)

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| ACCT-01 | accounts/models.py:309-356 | P2 | RBAC | `capability_defaults_for_role` is applied "on invite" only. Confirm a later **role change** (e.g. SALES_STAFF → ACCOUNTANT) re-applies the capability set; otherwise a promoted user silently lacks `can_post_journals` / `can_create_purchases` (or an demoted user keeps elevated flags). |
| ACCT-02 | accounts/models.py:109 vs 262-294 (`CompanyGstin`) | P2 | dual source of truth | `Company.gstin` (scalar) and `CompanyGstin` rows coexist. `is_gst_registered` reads the new model; `core.services.billing`, `document_numbers`, GSTR code frequently read `company.gstin` directly. A tenant migrated to multi-GSTIN who blanks `company.gstin` breaks every scalar reader. |
| ACCT-03 | accounts/otp_utils.py:16-25 | P3 | crypto | OTP digest is `HMAC-SHA256(pepper, code)` with a **global** pepper and no per-challenge salt — identical 6-digit codes across users produce identical digests (cross-user precompute if the pepper leaks / defaults to `SECRET_KEY`). Low impact given short TTL + verify throttle, but a random per-challenge salt column is cheap. |
| ACCT-04 | accounts/models.py:312-325 | P3 | RBAC | ACCOUNTANT preset has `can_cancel_documents: False` — an accountant cannot reverse/cancel a mis-completed invoice they can otherwise post journals for. Confirm this is intended (owner-only cancel). |

---

## 11. masters — HSN rate catalog / pricing

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| HSN-01 | masters/hsn_catalog.py:108-132 + billing.py:401-416 | P1 | GST correctness | `STARTER_HSN_RATES` (the seed for `HsnRate`, which `rate_for()` reads and `apply_effective_gst_rate` uses to **override** the invoice line rate) contains exactly **two** HSN codes: `1905` and `2402`. For every other HSN `rate_for()` returns `None` and the product/line rate is used as-is. Net effect: the GST 2.0 (22 Sep 2025) rate changes are auto-applied only to biscuits and cigarettes; the thousands of other goods that changed slab are not touched, and `validate_gst_rate` still accepts the now-legacy `12`/`28` values — so a product master last edited in 2024 keeps quoting a defunct rate with no warning. Either ship a real catalog or drop the "date-effective statutory rate" claim. |
| HSN-02 | masters/hsn_catalog.py:78-91 | P2 | UX | The "Find HSN" helper (`search_hsn`) shows a GST rate for only the 2 codes in `STARTER_HSN_RATES`; the other ~75 curated `COMMON_HSN` entries display a blank rate. A user picking an HSN from the helper gets no rate guidance for almost everything. |
| HSN-03 | masters/hsn_catalog.py:150-161 | P3 | data integrity | `rate_for` has no guard against two `HsnRate` rows with the same `hsn_sac` and overlapping validity windows; it just picks the latest `valid_from`. |
| PRICE-01 | masters/pricing.py:22-56 | P3 | correctness | `assert_slab_payloads` checks overlap only *within one payload*; it does not re-check against slabs already persisted for that product on the same price list, so two separate save calls can create overlapping qty slabs. |

## 12. billing (SaaS subscription) + write gate

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| SUB-01 | billing/services.py:24-33 + middleware.py | P2 | migration hazard | `company_writes_blocked` returns `REQUIRE_SUBSCRIPTION` (True in production) when a company has **no** `Subscription` row. Any tenant created before billing existed, or one whose `ensure_register_trial` never ran, is fully write-blocked after deploy until a subscription row is manually created. Needs a data migration that back-fills trial/grandfathered subscriptions. |
| SUB-02 | billing/middleware.py:67 + services.py:14-21 | P2 | data portability | A suspended tenant is hard-blocked from every non-safe request except `/auth`, `/billing`, `/health`, `/help-*`. If tenant data export / backup is a POST endpoint (it is not in `ALLOW_PREFIXES`), a suspended customer cannot get their own data out. Add the export endpoint to the allow-list. |
| SUB-03 | billing/middleware.py:21-67 | P3 | perf | `SubscriptionWriteGateMiddleware` runs an uncached `subscription_for_company` query on every mutating request (a 4th auth attempt also happens here — RLS middleware, this middleware, and DRF each call `CookieJWTAuthentication`). Cache the subscription on the request alongside `_company_user`. |
| SUB-04 | billing/services.py:202-214 | P3 | lifecycle | `_map_razorpay_status` maps `cancelled`/`completed`/`expired` → SUSPENDED and returns `None` (no-op) for `pending`/`authenticated`. A subscription that Razorpay leaves `pending` (failed auto-charge retry) is never moved to PAST_DUE by the webhook — it silently keeps its prior status. |
| SUB-05 | billing/views.py:104-162 | P3 | replay | The Razorpay billing webhook verifies the HMAC signature (good) but has no event-id dedup/freshness window; a replayed `subscription.halted` for an id that has since been re-subscribed under a *new* id is harmless, but a replayed event for the *same* id re-applies state. |

## 13. integrations / banking / payroll / manufacturing (preview modules)

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| PR-01 | payroll/services.py:16, 161-170 | P2 | statutory (preview) | Employer PF is modelled as a flat 12%. Statutory employer share splits 8.33% EPS (capped at ₹15 000 wage → ₹1 250) / 3.67% EPF, plus EPF admin + EDLI charges. The EPS/EPF split and admin charges are absent — ECR-grade numbers are wrong. |
| PR-02 | payroll/services.py:19, 103-128 | P2 | statutory (preview) | `DEFAULT_PT_SLABS` deducts a flat ₹200/month Professional Tax for **every** state not in the 6 hard-coded ladders — including Delhi / UP / Haryana / Uttarakhand, which levy **no** professional tax. A no-PT-state company over-deducts from every employee. |
| PR-03 | payroll/services.py:179 | P2 | statutory (preview) | Salary TDS (sec. 192) is a flat `gross × tds_rate%`. Real 192 TDS is annual liability (regime slabs, standard deduction, 87A rebate, Ch. VI-A) ÷ remaining months. The flat rate is a manual guess with no annualisation. |
| INTG-01 | banking/fiu_adapter.py:47-98 | P2 | not implemented | "Live" Account Aggregator ingest is a plain `GET {FIU_BASE_URL}/consents/{id}/transactions`. The real AA/ReBIT flow (consent artefact, FI data request, ECDH key material, encrypted FI push, decryption) is not present — this only works against a hypothetical proxy. (Settings label AA as preview/dark — consistent, but the "live" path is a stub.) |
| INTG-02 | banking/services.py:14-58 | P2 | ineffective feature | `match_aa_to_receipts` only auto-matches when `CustomerReceipt.reference`/`utr` exactly equals `AaTransaction.txn_id` (the bank's internal id — a different identifier from the customer-entered UTR), with an exact-paise amount and ±7-day window. In practice it will match almost nothing; there is no amount+date candidate fallback and no narration UTR parse. |
| INTG-03 | banking/services.py:19-27 | P2 | locking | `match_aa_to_receipts` holds `select_for_update()` on all unmatched AA rows *and* takes `select_for_update()` on candidate `CustomerReceipt` rows inside a per-row loop — a long transaction that locks many receipts for the duration. |
| TALLY-01 | integrations/tally/adapter.py (whole) | P2 | scope (disclosed) | Tally support is one-shot CSV/Excel import + an export dump, explicitly "not live sync, not certified parity" (`DISCLAIMER`). Fine as disclosed, but the product surface should make sure the UI never implies round-trip sync. |

---

## 14. web/ frontend (React/TS) — money math parity, API client, forms, UX

*(Codebase is disciplined: 0 `console.log`, 0 `TODO`, ~1 `as any`, 3 `@ts-*`, no `dangerouslySetInnerHTML`. Findings are about behaviour/UX, not hygiene.)*

| ID | Loc | Sev | Cat | Finding |
| :- | :- | :- | :- | :- |
| FE-01 | web/src/utils/money.ts:7-30, tax.ts:36 | P2 | FE/BE parity | All FE invoice math is float64 through `roundMoney` (`toFixed(10)` + BigInt cents). The module comment concedes it "can differ from the posted document by ~1 paise". Users editing an invoice see the total shift when they hit Complete (FE preview → server total). Acceptable only if the UI *always* shows a "server totals are authoritative" reconciliation and never lets the FE number be the one that's saved. |
| FE-02 | web/src/utils/tax.ts:262-280 | P2 | FE/BE divergence | FE `calculateInvoiceTotals` allocates a `BEFORE_TAX` invoice discount by **dumping the residual on the last line** (`i === working.length - 1`). The backend (billing.py R1-020) explicitly moved to *spreading* the residual across lines with headroom. Per-line taxable/tax in the FE preview will not match what the server posts for BEFORE_TAX-discounted invoices. |
| FE-03 | web/src/utils/tax.ts:199 | P2 | FE/BE divergence | `extractExclusiveFromInclusiveLine` drops `cessRate` from the extraction denominator when a per-unit `cessAmount` is present (`specific > 0 ? 0 : cessRate`). The backend keeps `gst_rate + cess_rate` in the denominator and peels the specific cess separately. Inclusive-priced lines carrying both ad-valorem and specific cess get a different exclusive unit price / taxable on FE vs BE. |
| FE-04 | web/src/utils/tax.ts:79-82, 154-156 | P2 | GST | FE mirrors the backend's asymmetric intra-state split (`cgst = round(tax/2)`, `sgst = round(tax) - cgst`) — so it inherits BILL-01 (CGST ≠ SGST by a paise), and the FE preview will show that asymmetry to the user on-screen. |
| FE-05 | web/src/api/client.ts:27-32 | P2 | UX | A blanket `timeout: 30000` on the shared axios instance. GSTR pack builds, tenant exports, and bill-OCR calls routinely exceed 30 s — the client aborts and shows a timeout while the server keeps working (and may double-charge idempotency budget on retry). Timeouts should be per-request. |
| FE-06 | web/src/api/client.ts:46-83 + separate API/web hosts | P2 | deploy fragility | Mutations depend on a readable `csrftoken` cookie fetched from `/auth/csrf/`. With API and SPA on different hosts (the documented deploy) that cookie is third-party; Safari ITP / "block third-party cookies" / Brave will drop it, and `ensureCsrfCookie` then throws `"CSRF token is unavailable"` on *every* write. Needs either same-site hosting, a double-submit token in a response body, or a documented cookie requirement. |
| FE-07 | web/src/pages/sales/NewInvoicePage.tsx:1025-1029 | P2 | UX blocker | `canComplete` requires `(!previewOnline || preview.ready)`. If the `/preview` endpoint is reachable but keeps erroring (500), `preview.ready` stays false and **Complete is permanently disabled** with no "complete with client totals" fallback — a working invoice cannot be saved. |
| FE-08 | web/src/pages/sales/NewInvoicePage.tsx:1012-1021 | P2 | data loss | The unsaved-invoice guard is `beforeunload` only. Clicking an in-app nav link (React Router) with a half-built invoice discards it silently — no `useBlocker`/confirm on client-side navigation. |
| FE-09 | web/src/pages/sales/NewInvoicePage.tsx:1098-1104 (`onSignaturePick`) | P2 | surprising side-effect | Picking a signature image on the invoice screen calls `updateCompany({ signature: uploaded.id })` — it changes the **company-wide default signature** for all future invoices, not just this one. |
| FE-10 | web/src/pages/sales/NewInvoicePage.tsx:1023 (`activeCustomers`) | P2 | scale | The customer picker filters `customers.data?.results` — page 1 only (≤50, fewer after the ACTIVE filter). A company with >50 customers can't select most of them unless the picker also does server-side search. Same pattern likely in NewPurchasePage supplier picker. |
| FE-11 | web/src/App.tsx:51-140 (lazy routes) | P2 | PWA white-screen | ~90 routes are `lazy()` + `Suspense` with (per the head of the file) no route-level `ErrorBoundary`. After a deploy, a client holding the old `index.html` requests old hashed chunk names → 404 → `lazy()` rejects → white screen until a manual hard reload. Add an ErrorBoundary that offers "reload". |
| FE-12 | web/src/offline/invoiceDraftCache.ts:27-28, 144-158 | P2 | data at rest / loss | Offline invoice drafts (customer, amounts) are plaintext in IndexedDB/localStorage on shared POS devices (disclosed BB-000572). On `QuotaExceededError` `writeLocal` falls into a truncation path — confirm it never silently drops a cashier's queued sale. |
| FE-13 | web/src/offline/invoiceDraftCache.ts:28, 354-390 | P2 | offline sync | Outbox flush leans on Background Sync (unsupported on iOS Safari) + `navigator.locks` (older Safari lacks it). On iPad POS, queued offline invoices flush only on app reopen — ensure a always-visible "N unsynced" indicator so sales aren't stranded. |
| FE-14 | web/src/auth/session.ts:11-27, 99-101 | P3 | UX | `hasStoredSession()` keys off a stale `localStorage['bizboard.user']`. If the refresh cookie has expired, the app renders the authenticated shell then bounces to login on the first 401 — a flash of the app before the redirect. |
| FE-15 | web/src/auth/session.ts:45-53 (`clearTokens`) | P2 | session | `clearTokens()` only clears local state. Any component calling it directly (rather than an `/auth/logout/` that blacklists the refresh JWT) leaves the httpOnly refresh cookie valid server-side. Confirm every logout path hits the server. |
| FE-16 | web/src/pages/**/*.tsx (e.g. InvoiceDetailPage.tsx:871,966) | P2 | i18n | `hi.ts` exists, but newer pages carry hard-coded English JSX literals ("Filing identity", "Amend filing identity", …). Hindi users hit untranslated strings on those screens. No lint rule catches raw JSX text. |
| FE-17 | web/src/api/client.ts:186-188 | P3 | auth edge | The 5 s post-refresh debounce returns `'cookie'` (treated as success) to a 401-retry even if the access cookie genuinely expired again in that window; with a very short `JWT_ACCESS_MINUTES` this can make a retry give up instead of refreshing. |
| FE-18 | web/src/api/client.ts:113-121 | P3 | fragile | `isCsrfFailure` decides whether to auto-retry a 403 by running `/csrf/i` over the stringified body. If the backend CSRF message wording changes, first-mutation 403s stop auto-recovering. |
| FE-19 | web/src/pages/sales/NewInvoicePage.tsx:1063-1078 | P3 | UX | `Ctrl+Enter` completes the invoice from focus in any non-dialog `<input>` (only TEXTAREA / contentEditable are excluded) — a hard, mostly-irreversible action on a stray keystroke. |
| FE-20 | web/src/components/PageState.tsx | P3 | perceived perf | Every list/detail page shows a centered `CircularProgress`; no skeleton screens. Fine, but skeletons markedly improve perceived load on the data-heavy list pages. |

---

## 15. Cross-cutting / systemic

| ID | Area | Sev | Finding |
| :- | :- | :- | :- |
| SYS-01 | tenancy | P1 | Tenant isolation is **app-level `.filter(company=…)` only** in the default deploy (`POSTGRES_RLS_ENABLED` off). One forgotten filter in any `get_queryset` = cross-tenant read/write. RLS exists but is un-deployed and only covers 8-ish tables. Decide: turn RLS on for every `CompanyScopedModel` table + a test that asserts coverage, or accept and document the single-layer risk. |
| SYS-02 | money math | P1 | Two independent implementations of the invoice tax/discount/rounding engine (Python `Decimal` in `core.services.billing`, TS float in `web/src/utils/tax.ts`) that must stay bit-compatible. They already diverge on BEFORE_TAX discount allocation (FE-02), inclusive+cess extraction (FE-03), and `discount_total` reporting (BILL-04). Every future change needs a paired-change checklist + a shared fixture test, or the FE preview keeps drifting from the posted document. |
| SYS-03 | `X or Y` Decimal fallbacks | P2 | A recurring pattern across billing/accounting/reporting: `Decimal(str(getattr(x, "field", 0) or 0))` and `taxable_amount or line_total`. `Decimal("0.00")` is falsy, so a legitimately-zero value silently falls through to the next term (often a tax-inclusive figure or a different field). Replace `A or B` with explicit `A if A is not None else B`. |
| SYS-04 | reversal dating | P2 | Document-correction flows (`adjust_*_postings`, IMS reclass, note posting) date the *reversal* leg to `timezone.localdate()` while the *re-post* uses the source document date. Any report window between the two dates double-counts or misses the correction (ACC-02). One rule: a correction's reversal inherits the original entry's `entry_date`. |
| SYS-05 | "no defined period ⇒ anything goes" | P2 | Every period gate (`PostingService.post`, `assert_period_allows_money_amend`) only blocks dates inside an explicitly closed period row. A tenant that never closes anything has zero back-dating / prior-period protection (ACC-04, PER-03). |
| SYS-06 | idempotency table growth | P2 | `IdempotencyRecord` is durable-by-design with no janitor task; money-scope keys are never reclaimed even when stale (CORE-04/05). A crash between commit and `store_record` bricks that key forever. Needs a TTL + a resolve-by-`resource_id` path. |
| SYS-07 | preview modules oversold | P2 | Payroll (PF/ESI/PT/TDS), CRM, Account-Aggregator "live", PayU, and the HSN rate catalog are stubs or materially simplified, but appear as first-class menu items. Ensure each carries an in-product "preview / not filing-grade" banner so a user doesn't run payroll off wrong statutory numbers. |
| SYS-08 | external HTTP in request path | P2 | Razorpay/Cashfree/PayU link creation and the Razorpay subscription create are synchronous 15-30 s HTTP calls with no retry/breaker. Confirm none hold a DB transaction open; move link creation to a task with a "pending" UI state. |
| SYS-09 | error-envelope gaps | P2 | `api_exception_handler` doesn't map Django `ValidationError` raised outside serializers, and `EnvelopeJSONRenderer` mis-detects any payload with a boolean `success` field as "already wrapped" (CORE-06/08). Both produce malformed responses the SPA can't parse. |
| SYS-10 | GST 2.0 readiness | P1 | `validate_gst_rate` still accepts `12` and `28`; the HSN catalog that would re-rate items to the Sept-2025 slabs has 2 rows (HSN-01). Nothing nudges a tenant to refresh product masters after the reform — invoices dated post-cutover can silently carry defunct rates. |

---

## Appendix — coverage & method

**Read in full / near-full:** config/settings.py, core (middleware, rls + one policy migration, authentication, exceptions, permissions, throttles, validators, models, renderers, pagination, serializers, tasks, views), core.services.billing/charges/place_of_supply/document_numbers/feature_flags/h9_amend, accounting (models, services all 1577 lines, books-health), reporting.gst_returns (GSTR-1 in full, 3B/9 spot), reporting.gst_periods, payments (models, gateway all 4 providers, webhook_views), billing (services, views, middleware), inventory.services valuation engine, ledgers.services outstanding, accounts (models, otp_utils), masters (pricing, hsn_catalog), payroll.services, banking (fiu_adapter, services), web (utils/money, utils/tax, api/client, auth/session, App.tsx routing head, NewInvoicePage save path, offline/invoiceDraftCache structure).

**Skimmed / structure only:** sales.services & serializers (Complete/cancel/amend paths traced), purchases.services, imports.services (2736 lines), insights.*, integrations.tally.adapter, manufacturing.services, crm.*, most of web/src/pages, mobile/ (Capacitor shell — 17 lines).

**Not reached (recommend a follow-up pass):** imports pipeline line-by-line (LLM bill extraction, 2736 LOC), insights assistant/attention/alerts logic, e-invoice/e-way (`irn_guard`, `EinvoiceEwayPanel`), the full DRF view layer for authz-per-action across every module, `tenant_backup.py` (1200 LOC restore path), search app, POS page (1382 LOC), the ~300-file web/src/pages tree for per-screen UX, accessibility audit (labels/focus/keyboard/contrast) beyond spot checks, i18n completeness sweep, test-suite quality.

