# W0-08 — Ops integrity

2026-08-31 | DONE | local | Gunicorn still drops to app via setpriv (never stays root). CD pins image digests after push and fails if sha256 missing. OPENING_STOCK unique per warehouse/product/batch excluding import_voided. Product import void persists _import_created so pre-existing SKUs stay active.
