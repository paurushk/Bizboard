# C-03 — Find stock the way the shop talks

2026-08-31 | DONE | local | Active custom fields are default item-list columns and POS/search keys. `custom_fields.<key>` (and `custom_fields.brand` → brandCode) filters the product API. Track-batch Complete without a picked lot uses FEFO. Expiry alerts Write off posts the existing ADJUSTMENT / EXPIRED movement.
