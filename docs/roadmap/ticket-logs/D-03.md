# D-03 — Invoice audit trail

2026-08-31 | DONE | local | GET /sales/invoices/{id}/audit/ returns company-scoped AuditEvent timeline (Owner/CA). Invoice screen shows who/when/money diffs from metadata. Sales staff 403.

# D-01 — Accountant practice workspace

2026-08-31 | DONE | local | Multi-membership with no active_company returns 409 COMPANY_REQUIRED + memberships (no silent auto-pick). Single-membership still auto-selects. AUTO_PICK_COMPANY_ON_EMPTY restores old pick. SPA dialog + interceptor (no retry loop). Cashier home already skips P&L when financial reports are off.
