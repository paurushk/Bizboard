# X-01 — Performance budget and 50k-invoice soak

2026-08-31 | PARTIAL | local | SLOs written in `load/README.md` (Complete p95 <800ms, list <2s, dashboard <500ms on a 50k tenant, ex-PDF/ex-GSP). `load/k6_slo.js` + `load/SEED_50K.md` added. Soak not run on this machine — no numbers claimed.

2026-09-12 | RUN, MISSES SLO | local dev laptop, NOT staging | QOS-0003: seeded a real 50k-`COMPLETED`-invoice tenant (`manage.py seed_synthetic_bulk --invoices 50000`) against a single-container Postgres 17 on a Windows dev laptop and ran `load/k6_slo.js` (10 VUs list / 5 VUs complete, 2m). Numbers, not claims:
  - **Invoice list p95 = 9.33s** (target <2s) — misses SLO on this hardware. Real number, but this is a laptop-hosted single Postgres container, not the staging infra the SLO targets; do not read this as "the app is 4.5x too slow in production," only as "not yet validated on real infra."
  - **Invoice Complete — number not usable as an SLO signal.** The script drives repeated `POST .../complete/` against one pre-created draft; only the first call can actually complete it (`200`), every subsequent call correctly gets `400 already completed` in ~70-200ms. 314/315 of the "complete" samples in this run are conflict-rejection latency, not real Complete work — p95=924ms is not a meaningful data point. **Script gap, not just an environment gap:** `k6_slo.js`'s `completeDraft()` needs either a pool of pre-created drafts (one per call) or a per-VU draft created in its own `setup()`/exec to measure real repeated Completes — tracked as a follow-up, not fixed in this pass.
  - Fixed while running this (QOS-0003, shared by `k6_smoke.js`): both scripts previously re-logged in on every iteration, tripping Bizboard's own 10/min login throttle within seconds of any run with real credentials — a false failure unrelated to performance. Both now authenticate once in `setup()`.
  - Also ran `load/k6_smoke.js` (health + auth + list, 5 VUs/30s) against the same stack: **green** — 0% failed, p95=93ms. This is what `.github/workflows/ci.yml`'s `load-harness` job runs; it had never been confirmed to pass end-to-end before (see `qos/backlog/QOS-0003.yaml`).
  - `backend/tests/test_qos0003_large_tenant_reports.py` (3 tests) re-run against real Postgres (previously only verified on SQLite): **3/3 pass**, 371.95s wall-clock including fixture setup.
  - **Still open:** a real staging-infra soak (this run used a laptop, not a production-shaped host) and a `k6_slo.js` fix for the Complete scenario. Neither is claimed done here.

2026-09-17 | SCRIPT FIX, NOT RUN | — | QOS-0003 follow-up: fixed `completeDraft()`'s script gap noted above. Added `manage.py seed_draft_pool --count N` (creates N real DRAFT sales invoices via `SalesService.set_items`, topping up stock first) and changed `k6_slo.js` to draw one draft per call from `DRAFT_INVOICE_IDS` (a `SharedArray`, indexed by `exec.scenario.iterationInTest` — a k6-native counter unique across the scenario's VUs, so every VU gets a disjoint slice of the pool with no re-completion collisions). `DRAFT_INVOICE_ID` (singular) still works as the old single-draft fallback. **Not run against a live backend in this pass — no new p95 number, code change only.** Still open: the real staging-infra soak, and confirming this fix in practice (does the pool actually get consumed as expected end-to-end).
