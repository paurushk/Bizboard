# W0-04 — Document numbers are one FY policy

2026-08-31 | PARTIAL | local | PD-03 code: any GSTIN keys series by GSTIN+FY even if the caller omitted gstin; allocate still nested in Complete’s transaction; FY restart warning; cancelled-number register GET /api/v1/reports/cancelled-document-numbers/?fy= (JSON+CSV). PD-03 CA signature is still blank — merge blocked until signed.
2026-08-31 | PARTIAL | local | Review: cancelled-number register `fy` must match YYYY-YY (e.g. 2026-27) so the CSV filename cannot carry extra header characters.
