# W0-06 — Valuation is a ledger, not a full replay

2026-08-31 | DONE | local | Perpetual WAVG InventoryRunningCost on post_movement; unit_cost no longer replays all movements. Historical as_of uses month-end snapshot when movement count > 10,000 then replays after that period. valuation_business_date_order default off for existing tenants (new register defaults on). rebuild_running_cost fails on StockBalance qty drift. FIFO still peel-based; flag-off as_of stays created_at order.
