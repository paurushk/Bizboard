# Findings register — 5 Sep 2026

Independent re-read of current source (not the 3 Sep catalog). **IDs `R-001`…`R-088` are the open defects.** `R-089`…`R-098` are implementation hints, not extra defects — each maps to a parent below.

Companion plan: [`FIX_PLAN_2026-09-05.md`](./FIX_PLAN_2026-09-05.md).  
Interactive views (IDE only, not git): the 5 Sep review canvas and fix-plan canvas under the local Cursor `canvases/` directory.

**Census: 5 P0 · 27 P1 · 24 P2 · 18 UX · 14 PARTIAL = 88 open.** Plus 10 suggestions. Plus 15 already-fixed notes (F-01…F-15) — do not rework.

---

## Open findings (R-001 … R-088)

| ID | Sev | Module | Title | Where | One-sentence |
|----|-----|--------|-------|-------|--------------|
| R-001 | P0 | Sales | TCS rate never applies — UI always sends tcsAmount | `NewInvoicePage.tsx` · `sales/serializers.py` · `billing.py` | Payload always includes `tcsAmount:0`, so the serializer marks TCS manual and rate math never runs. |
| R-002 | P0 | Payments | `refund.failed` classified as REFUNDED | `payments/gateway.py` · `webhook_views.py` | Any `refund.*` event unwinds allocations while the capture stays with the merchant. |
| R-003 | P0 | Payments | `refund.created` + `refund.processed` both unwind | `gateway.py` · `payments/services.py` | Distinct Razorpay event ids mint two skip_gateway unwinds for one partial refund. |
| R-004 | P0 | Accounting | Period Close UI bypasses health/WIP gates | `PeriodsPage.tsx` · `accounting/serializers.py` | UI PATCHes `status`; gated `POST /soft-close/` and `/close/` are never called. |
| R-005 | P0 | Auth/RLS | Anonymous payment webhooks see zero rows when RLS is on | `payments/webhook_views.py` · `core/middleware.py` | AllowAny + `app.company_id=None` + FORCE RLS on `PaymentLink` / `ProcessedWebhookEvent`. |
| R-006 | P1 | Sales | Sales CN/DN never RCM-memoize | `sales/notes_services.py` vs purchases twin | RCM invoices store GST in memo; sales notes still include GST in grand_total. |
| R-007 | P1 | Sales | Sales return UI cannot capture serials | `SalesReturnsPage.tsx` | Create payload omits serials the backend requires on Complete. |
| R-008 | P1 | Purchases | Purchase return UI omits serials/lots | `PurchaseReturnsPage.tsx` | Same stripped payload; Complete requires serials/lots when tracked. |
| R-009 | P1 | POS | POS never sends serials, batches, or warehouse | `PosPage.tsx` | Serial SKUs cannot complete at the counter. |
| R-010 | P1 | Sales/Purchases | `completeWithConfirms` only handles two codes | `completeWithConfirms.ts` | `confirm_no_rcm`, duplicate-bill, and additional-debit are never prompted. |
| R-011 | P1 | Purchases | Purchase serializer omits `rcm_cess` | `purchases/serializers.py` | Model has `rcm_cess`; API/PDF/GSTR clients cannot see it. |
| R-012 | P1 | Purchases | Purchase writes drop `SubscriptionWritesAllowed` | `purchases/views.py` vs sales | Lapsed subscription can still post purchases, stock, and AP. |
| R-013 | P1 | Inventory | Product mutate UI allows any non-viewer | `ProductsPage.tsx` | `canMutate = !isViewer`; API requires `CanManageInventory`. |
| R-014 | P1 | Auth | Tenant export ≪ wipe; no SPA for encrypted backup | `tenant_backup.py` · `BackupExportPage.tsx` | Wipe deletes models export does not restore; Owners only see CSV registers. |
| R-015 | P1 | Core | Empty plan `modules={}` fail-opens dark modules | `feature_flags.py` · `billing/services.py` | Trial `{}` is falsy, so env `ENABLE_*` grants CRM/payroll/mfg. |
| R-016 | P1 | GST | GSTR-3B hero looks like a filing payable | `GstReturnPage.tsx` | “Net GST Payable” is a worksheet figure, including provisional ITC. |
| R-017 | P1 | GST | CMP-08/GSTR-4 UI stubs while APIs exist | `GstReturnPage.tsx` · `reporting/` | Composition tenants cannot use built worksheets; 6/7/8 are honesty dumps. |
| R-018 | P1 | Payments | `allocate_receipt` skips the period lock | `payments/services.py` | Reverse enforces `assert_period_allows_money_amend`; allocate does not. |
| R-019 | P1 | Accounting | Missing-posting scan omits PCN/PDN/PR/SR/BoE | `accounting/services.py` | Completed notes/returns/BoE with no JE never block period close. |
| R-020 | P1 | Payments | Cashfree/PayU fee always 0 | `payments/gateway.py` · `recon.py` | MDR never posts; recon scores gross vs net bank credit. |
| R-021 | P1 | Auth | Invite resend uses company id as membership pk | `accounts/views.py` | `CompanyUser(..., pk=active_company_id)` is the wrong key. |
| R-022 | P1 | Auth | OTP “sent” after SMS failure; cooldown already burned | `accounts/views.py` | Uniform 200 is intentional anti-enum; known users are locked out with no SMS. |
| R-023 | P1 | Purchases | Purchase TDS rate never computes amount | `NewPurchasePage.tsx` · payable uses `tds_amount` | Rate-only TDS leaves full AP (no TCS-style fold). |
| R-024 | P1 | Purchases | Import of goods hard-blocked; BoE has no UI | `purchases/services.py` · `boe_services.py` | Complete raises “not supported” while BillOfEntry APIs exist. |
| R-025 | P1 | Sales | CN qty caps only when `source_item` is set | `sales/notes_services.py` | Omitting lineage credits more units than sold. |
| R-026 | P1 | Purchases | Purchase returns have no return-date vs invoice-date gate | `sales/return_service.py` vs `purchases/services.py` | Sales blocks return before invoice date; purchases do not. |
| R-027 | P1 | Sales | RCM preview zeros tax; sales has no liability panel | `sales/views.py` · `NewInvoicePage` | Preview returns ₹0 tax; purchase RCM alert then reads that zero. |
| R-028 | P1 | Accounting | OPEN_WIP includes `released_at IS NULL` forever | `accounting/services.py` | Any undated RELEASED work order blocks every period close. |
| R-029 | P1 | CRM | CRM nav is Owner-only; routes/API allow sales | `menu.ts` vs `App.tsx` vs `crm/views.py` | Three different ACL rules; sales staff deep-link with no nav. |
| R-030 | P1 | Sales | Recurring invoices create drafts only | `sales/recurring.py` | Operators expecting auto-invoice must still Complete each run. |
| R-031 | P1 | Billing | SaaS Razorpay webhook has the same RLS hole | `billing/views.py` | AllowAny insert of FORCE-RLS `ProcessedWebhookEvent` with GUC=None. |
| R-032 | P1 | Purchases | RCM re-zero path does not clear line cess | `purchases/services.py` | Stale cess remains on lines when the header was already memoized. |
| R-033 | P2 | Auth | Seat limit counts active members only | `accounts/views.py` | Pending invites (`is_active=False`) do not consume seats. |
| R-034 | P2 | Auth | Login lock trusts first X-Forwarded-For hop | `accounts/views.py` `_client_ip` | Spoofed leftmost IP rotates the lock or pins a victim. |
| R-035 | P2 | Core | Body size guard is Content-Length only | `core/middleware.py` | Chunked / undeclared bodies bypass the 413 ceiling. |
| R-036 | P2 | Masters | Catalog list cache is not invalidated on mutate | `masters/views.py` | 60s TTL; PATCH does not `cache.delete`. |
| R-037 | P2 | Masters | Overlapping price slabs allowed | `masters/pricing.py` | No max&lt;min or overlap check; matcher picks highest min_qty. |
| R-038 | P2 | Purchases | Purchase list filters pass raw query strings | `purchases/views.py` | Sales validates ISO dates; purchases can 500. |
| R-039 | P2 | Inventory | Purchase-return lot replay is LIFO vs FEFO comment | `purchases/services.py` | `order_by('-id')` instead of `fefo_batches`. |
| R-040 | P2 | Core | CSV formula prefix misses tab/CR | `core/csv_utils.py` | Leading `\\t` / `\\r` still inject into Excel. |
| R-041 | P2 | GST | Sticky GSTR-2B MATCHED skips rematch | `reporting/gstr2b.py` | Corrected PI amount/date/GSTIN never rematches. |
| R-042 | P2 | Payments | Dunning fires only on exact overdue day | `payments/dunning.py` | Quiet hours skip the bucket with no catch-up. |
| R-043 | P2 | Banking | AA/recon matching is first-hit / first-50 | `banking/services.py` · `recon.py` | Unordered cap; first `select_for_update` wins. |
| R-044 | P2 | Insights | Alert builders swallow errors; GET summary writes | `insights/alerts.py` · `views.py` | Alerts vanish; dashboard polling writes on every load. |
| R-045 | P2 | Frontend | Any refresh failure clears session and drafts | `web/src/api/client.ts` | Transient 4G on refresh → logout + draft purge. |
| R-046 | P2 | Frontend | Offline CSRF fetch failure still sends mutate | `web/src/api/client.ts` | Request continues without the header → 403. |
| R-047 | P2 | Inventory | Stock count/transfer offline has no auto-flush | Stock count / transfer pages | Drafts sit until `/offline-outbox`. |
| R-048 | P2 | Core | Preview can emit rate-override audit events | `core/services/billing.py` | Quote-only preview writes `AuditService`. |
| R-049 | P2 | Payments | Bank CSV blank date rows are silently skipped | `payments/recon.py` | Unlike bad date/amount, blank date is not in `skipped[]`. |
| R-050 | P2 | GST | GST soft-close and accounting close are separate | `gst_periods.py` vs `AccountingPeriod` | Money gate checks both; UI can leave one OPEN. |
| R-051 | P2 | Auth | Tenant export rate-limit is TOCTOU | `accounts/export_views.py` | `cache.get` then build then `cache.set`. |
| R-052 | P2 | WhatsApp | Cloud failures silently become wa.me; language `en` | `core/services/whatsapp.py` | HTTP ≥400 returns `mode=link`; Hindi templates degrade. |
| R-053 | P2 | Config | RLS default contradicts settings vs compose vs README | `settings.py` · compose · README | settings `'1'`; compose `:-0`; README off-until-proven. |
| R-054 | P2 | Inventory | `default_warehouse` can set a second DEFAULT | `inventory/services.py` | Race / IntegrityError when two defaults exist. |
| R-055 | P2 | Purchases | Purchase Complete keeps existing lot expiry on mismatch | `purchases/services.py` | Warns and keeps old expiry; item-stock path hard-errors. |
| R-056 | P2 | Core | `item_custom_fields_v2` defaults ON when key absent | `feature_flags.py` | Kill-switch requires explicit false. |
| R-057 | UX | Accounting | Periods chrome is hardcoded English | `PeriodsPage.tsx` | Name/Start/End/Close FY are not `t()`. |
| R-058 | UX | Sales | Return success flash is hardcoded English | Sales/Purchase returns pages | “Sales return completed” not localized. |
| R-059 | UX | Sales | TCS amount field label is English | `NewInvoicePage.tsx` | Neighbours are i18n’d. |
| R-060 | UX | GST | 3B subtitle is English | `GstReturnPage.tsx` | “Tax collected on Sales − ITC…” next to `t()`. |
| R-061 | UX | Sales | Bare `EmptyState` with no description | Returns pages | Generic empty; no next action. |
| R-062 | UX | GST | GSTR-6/7/8 are URL-reachable honesty dumps | `App.tsx` vs `menu.ts` | Bookmarks look like a broken report. |
| R-063 | UX | Payments | Account Aggregator nav is a dead-end | `AccountAggregatorPage.tsx` | Nav promises a feature with no consent UI. |
| R-064 | UX | Settings | Backup settings page is CSV registers | `BackupExportPage.tsx` | Encrypted zip APIs have no Owner UI. |
| R-065 | UX | Shell | Nested history routes do not highlight Sales History | AppShell nav | `/sales/history/123` ≠ `/sales/history`. |
| R-066 | UX | Sales | Return is create+complete in one gesture | `NewInvoicePage.tsx` | Serial failure is after the draft exists. |
| R-067 | UX | POS | POS confirms blank POS but not serial/batch failures | `PosPage.tsx` | Cashier recovery is inconsistent. |
| R-068 | UX | Auth | Register never returns a session | `accounts/views.py` · `auth.ts` | Successful register feels like it failed. |
| R-069 | UX | Auth | Login tabs lack an accessible name | `LoginPage.tsx` | `<Tabs>` without `aria-label`. |
| R-070 | UX | i18n | Missing `status.PARTIALLY_PAID` key | `i18n/en.ts` | Prod shows the dotted key. |
| R-071 | UX | Accounting | Journals/assets/transfer chrome remain English | phase pages | Hindi users hit English on advanced accounting. |
| R-072 | UX | Manufacturing | Work Orders look complete until serials fail at Release | `WorkOrdersPage.tsx` | Happy path hits `BusinessRuleError`. |
| R-073 | UX | GST | `books_provisional` can be read as file-this | `gstHonesty` 3B | Hero number still looks like a payable. |
| R-074 | UX | Settings | CSV export buttons use hardcoded “Export {type}” | `BackupExportPage.tsx` | Not `t()`. |
| R-075 | PARTIAL | Manufacturing | BOM + work-order MVP, not MES | `manufacturing/` | No MRP, routing, or shop-floor terminals. |
| R-076 | PARTIAL | Payroll | Salary voucher, not HRMS/statutory filing | `payroll/` | Payslips are not Form 24Q. |
| R-077 | PARTIAL | CRM | Convert + activities, not a pipeline | `crm/services.py` | ENABLE_CRM looks like CRM; it is a notebook. |
| R-078 | PARTIAL | Tally | Dump/commit, not live sync | `integrations/tally/adapter.py` | Concurrent commits race without row lock. |
| R-079 | PARTIAL | Mobile | Capacitor WebView shell, not a store app | `mobile/` · CI | Play internal only; 8h-offline is the PWA. |
| R-080 | PARTIAL | GST | Offline worksheets, not GSTN filing | `reporting/gst_returns.py` | Live NIC fail-closed until `GSP_CERTIFIED`. |
| R-081 | PARTIAL | POS | Invoice+receipt+thermal, not a retail suite | `PosPage.tsx` | No hold-and-recall or cash drawer. |
| R-082 | PARTIAL | PWA | Shell + draft outbox, not offline ERP | `web/src/pwa.ts` | `/api` not cached; outbox is plaintext IndexedDB. |
| R-083 | PARTIAL | Purchases | BoE and taxable purchase charges incomplete | `boe_services.py` | BoE has no UI; purchases lack `charges_hsn`. |
| R-084 | PARTIAL | i18n | `ta.ts`/`gu.ts` exist but are not in the switcher | `i18n/` | Repo looks like four locales; product has two. |
| R-085 | PARTIAL | Search | Universal search is parties/products/invoices only | `search/views.py` | Dark modules are invisible to search. |
| R-086 | PARTIAL | Inventory | Default valuation is WAVG; FIFO engines can disagree | `inventory/services.py` | BS inventory vs P&amp;L COGS can diverge. |
| R-087 | PARTIAL | Banking | AA ingest APIs exist; no consent UI | `AccountAggregatorPage` | Cannot turn on AA as a customer feature. |
| R-088 | PARTIAL | Insights | AI is assistive; GET may write | insights assistant + alerts | Do not treat insights as books. |

---

## Suggestions (not extra defects)

| Hint | Parent | PR | What it says |
|------|--------|----|----------------|
| R-089 | R-001 | 2 | Omit `tcsAmount` unless the operator edited the amount field. |
| R-090 | R-005, R-031 | 1 | Resolve the link under `rls_bypass`, then `set_rls_company`. |
| R-091 | R-002, R-003 | 1 | Classify `refund.failed` as FAILED; key skip_gateway on provider refund id. |
| R-092 | R-004 | 2 | Call `POST /soft-close/` and `/close/`; serializer `status` read-only. |
| R-093 | R-007–R-010 | 4, 5 | Extend confirm flags; capture serials on returns/POS or block those SKUs. |
| R-094 | R-006 | 3 | Mirror purchases `_apply_rcm_memo_if_linked` on sales CN/DN. |
| R-095 | R-014 | 10 | Export every wipe target; SPA; fail closed on unbacked models. |
| R-096 | R-015 | 9 | Treat `{}` like a plan that omits dark modules (fail-closed). |
| R-097 | R-016 | 8 | Do not present worksheet totals as filing payables. |
| R-098 | R-012 | 4 | Include `SubscriptionWritesAllowed` on purchase writes. |

---

## Already fixed in current tree (do not rework)

| ID | What | Evidence |
|----|------|----------|
| F-01 | Phone unique is E.164 on save | `User.save()` → `canonicalize_user_phone` |
| F-02 | Celery refund tasks pass `company_id` | `execute_gateway_refund.delay(..., company_id=)` |
| F-03 | Sales-return auto-CN remainder for discount/charges/TCS | `return_service.py` prior_qs aggregates |
| F-04 | H9-A blocks supply_nature and cess on completed amend | `h9_amend.py` |
| F-05 | `FeatureFlagsView` catches `CompanyRequired` | `core/views.py` |
| F-06 | WO serializer exposes batch/serial/expiry; UI collects serials | manufacturing serializers + `WorkOrdersPage` |
| F-07 | Payroll UI has basic/DA/TDS, LOP, cancel | Employees / PayRuns pages |
| F-08 | Locale switcher is English/Hindi only | `LocaleSwitcher` forces ta/gu → en |
| F-09 | PWA update confirms before reload | `pwa.ts` |
| F-10 | `ConfirmDialog` has confirming/disable-while-pending | `ConfirmDialog.tsx` |
| F-11 | Credit-note list Complete gated on `canCreateSales` | `CreditNotesPage.tsx` |
| F-12 | Offline invoice flush can post `amountReceived` | `useInvoiceOffline.ts` |
| F-13 | Offline outbox is in the nav | `menu.ts` |
| F-14 | API-path gateway refunds: seq key, `applied_refund_keys`, HTTP outside txn | `payments/services.py` — webhook skip_gateway is the remaining hole (R-002/R-003) |
| F-15 | PayU refund passes idempotency_key; Cashfree uses order-id helper | `gateway.py` — fee still hardcoded 0 (R-020) |

Sept-3 catalog `DEEP_CODE_REVIEW_2026-09-03.md` (~473 IDs) and `FIX_PLAN_2026-09-03.md` are **stale vs tree** for several Critical checkboxes (B4-001/002/003 API refund path is implemented; webhook path is not). Do not re-fix those API-path items.
