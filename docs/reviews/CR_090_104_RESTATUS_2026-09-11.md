# CR-090 … CR-104 — status re-verification (2026-09-11)

**Why:** `FUNCTIONAL_CODE_REVIEW_FINDINGS.md` (2026-09-08) carries 15 body entries
CR-090…CR-104 still labelled `Status: OPEN`. Its own header (updated later) and
the 2026-09-10 `FREEZE_SCOPE_COVERAGE.md` C15 sweep both say the code is clean,
but the body entries were never re-statused line by line. This is that pass —
each entry checked against the current working tree (Q-OS item **QOS-0057**).

**Result: 14 of 15 are FIXED in tree; CR-102 is a partial (mitigation added, root
cause open — tracked as QOS-0059).** The `Status: OPEN` labels in
`FUNCTIONAL_CODE_REVIEW_FINDINGS.md` are stale and should be updated to match.

| CR | Verdict | Evidence in tree | Regression test |
|---|---|---|---|
| CR-090 — POS inclusive tender preview omits `price_mode` | **FIXED** | `web/src/pages/pos/PosPage.tsx:1142` `previewSalesTotals({ price_mode, unit_price_inclusive })` mirrors the create payload (`:668`, `:679`) | POS suite (`posStatus.test.ts`) |
| CR-091 — online `cashPending` not durable across reload | **FIXED** | `PosPage.tsx:434` `// CR-091 …restore mid-settlement after reload`; `cashPending` restored + `enqueueDraft` (`:1201`); CR-106 resume path (`:1073`) | `web/src/pages/pos/posStatus.test.ts` `describe('POS cashPending session restore (CR-091)')`, `it('pos_upi_reload_mid_settlement_resumes_confirm')` |
| CR-092 — multi offline flush auto-prints last invoice only | **FIXED** | `PosPage.tsx:985` `thermalWarns: [] ` collected per draft in the flush loop (`:997` push), all handled at `:1017` | POS suite |
| CR-093 — sales DN complete does not lock source invoice | **FIXED** | `backend/sales/notes_services.py:365` `inv = SalesInvoice.objects.select_for_update().get(...)` in the debit-note path | notes/DN suite |
| CR-094 — cross-tenant `DeliveryChallan.sales_order` FK | **FIXED & VERIFIED** | `backend/sales/phase1_serializers.py:318` `validate_sales_order` → `check_company_ref` | `tests/test_a15_cr090_plus.py::test_cr094_delivery_challan_rejects_cross_company_sales_order` ✅ |
| CR-095 — `cancel_return` flips invoice status without a lock | **FIXED** | `backend/sales/return_service.py:322` `cancel_return` → `:421` `invoice = SI.objects.select_for_update().get(...)` | return suite |
| CR-096 — FE never sends CN confirm flags | **FIXED** | `web/src/utils/completeWithConfirms.ts:10-11` `confirmPaidInvoice` / `confirmPriceOverride` + mapping `:59-60` | `web/src/utils/completeWithConfirms.test.ts:69` "(CR-096)" |
| CR-097 — purchase cancel doesn't block draft CN/DN | **FIXED** | `backend/purchases/services.py:869` `# CR-097` blocks non-cancelled draft credit/debit notes | `tests/test_a15_cr090_plus.py::test_cr097_purchase_cancel_blocked_when_draft_credit_note_exists` ✅ |
| CR-098 — BoE cancel allowed while draft PIs link it | **FIXED** | `backend/purchases/boe_services.py:89` `# CR-098` nulls `bill_of_entry` on draft PIs | `tests/test_a15_cr090_plus.py::test_cr098_boe_cancel_unlinks_draft_purchase` ✅ |
| CR-099 — manual serial return peels first SALE move for product | **FIXED** | `backend/inventory/views.py:386` `_sale_movement_for_serial(company, serial)` — maps the serial's line index onto the peel that consumed that unit (docstring cites CR-099 / CR-142) | serial-return suite (`test_pr5_returns_serials_fefo.py`) |
| CR-100 — AP invoice outstanding sums all allocations | **FIXED** | `backend/ledgers/services.py:207` and `:862` `receipt__isnull=True,  # CR-100` on `purchase_invoice_outstanding` + `bulk_*` | `tests/test_a15_cr090_plus.py::test_cr100_purchase_invoice_outstanding_ignores_receipt_allocations` ✅ |
| CR-101 — dashboard payables GL while AR is document aging | **FIXED** | `backend/reporting/services.py:126` `_company_payables` → `_aging_total(payables_aging(company))` (docstring cites CR-101) | `tests/test_a15_cr090_plus.py::test_cr101_dashboard_payables_equals_aging_sum` ✅ |
| CR-102 — inventory summary `reserved` still from `StockBalance` cache | **PARTIAL** | `backend/reporting/services.py:566` `# CR-102` adds a `reserved_drift` flag (`row["reserved_drift"] = True` at `:594`), **but** `available = on_hand - reserved` (`:577`) still uses the cached `reserved`. Root cause (derive reserved from open reservations / movements) not done. | flag path only → **QOS-0059** |
| CR-103 — GL backfill treats empty POSTED JE as done | **FIXED** | `backend/accounting/management/commands/backfill_accounting_postings.py:29` `# CR-103: empty POSTED headers are not "done"` — `_has_je` checks lines | `tests/test_a15_cr090_plus.py::test_cr103_empty_posted_je_not_treated_as_done` ✅ |
| CR-104 — GST period soft_close has no `select_for_update` | **FIXED** | `backend/reporting/gst_periods.py:42` `# CR-104: lock the period row` + `select_for_update` in `soft_close_period` (`:47`) and the assert path (`:131`) | `tests/test_concurrency_races.py::test_concurrent_gst_soft_close_and_complete_race` (postgres marker) |

## Follow-ups

- **QOS-0059** (new): CR-102 residual — `reserved` on the inventory summary is still
  read from the `StockBalance` cache; only a drift flag was added. Derive it from
  open reservations / movements, or make `available` fall back safely when the flag
  is set.
- **Doc hygiene (not Q-OS scope):** update the 14 `Status: OPEN` lines in
  `FUNCTIONAL_CODE_REVIEW_FINDINGS.md` CR-090…CR-104 to `FIXED` / `PARTIAL` with a
  pointer to this file. That doc is under `reviews/` = history, so this companion
  file is the current record.
