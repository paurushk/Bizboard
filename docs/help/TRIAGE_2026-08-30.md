# Help triage 2026-08-30 (first weekly run)

**Owner:** Paurush  
**Window:** last 30 days  
**Input:** Settings → Help (`/settings/help`) — no production `helpV2` traffic yet (`helpV2` default off).

Zero-result queries to add as userQueries:
- none (no production `help_search` rows)

Worst intents by resolution rate (reword / diagnosis / product bug):
- none (no production `faq_resolved` / `faq_unresolved` rows)

Repeat-query offenders:
- none

Still-stuck backlog (Owner-visible HelpFeedback):
- none

Prevention notes to add/change:
- none — existing notes remain locked in `intents.ts`

New intent needed? no

**Outcome:** first run complete. Next run: fold into onboarding funnel review; add `userQueries` from `topZeroQueries` once a pilot company has `helpV2` on.
