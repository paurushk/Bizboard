import { en } from './en';

/** A-02b: Tamil money-namespace catalog. Unsigned / beta — falls back to English for review. */
export const ta = {
  ...en,
  locale: {
    ...en.locale,
    switchToTamil: 'தமிழ்',
  },
};
